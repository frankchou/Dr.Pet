# Phase 4-5 飲食頁「AI 智能分析配餐」結果畫面 — 全端施工報告

**角色：** 全端工程師
**日期：** 2026-06-10
**依據：** `docs/待辦清單.md` 4-5；設計圖 `docs/features/ui-mockups/diet-ai-analysis-01~06.jpg`、`diet-ai-analysis-report-01~05.jpg`
**動到的檔案：** `src/app/diet/page.tsx`（僅「AI 智能分析配餐 / 詳細報告」相關區塊）

> 4-4「新增項目」區（AddItemModal）未動；其他頁未動；未 commit；未動系統文件；未碰正式庫。

---

## 一、對照主畫面設計圖 01~06 的調整

### 標題列 + 評分（01/02）
- 左側加上深色腦圖示 + 主標「AI 智能配餐重點分析」+ 副標「核心指標匯總」（原為小標籤 + 「今日配餐評估」）。
- 右側大分數補上「分」字，維持綜合適配度說明。

### 核心指標（01/02）
- 由原本的單欄 4 列改為 **2×2 grid**，每格「標籤在上、數值在右、進度條在下」，配色貼近設計圖（蛋白橘 / 脂肪黃 / 鈣磷比墨綠 / 水分藍）。
- `NutrientBar` 元件改版：支援 `displayValue`（鈣磷比顯示「1.1:1」字串），版面改為標籤列 + 進度條。
- 補上設計圖的「個體建議容差 / 腎臟病專屬容差」圖例列。

### 配餐優化建議卡（03~06）
- 整塊包進淺色卡片，頂部加 **標題列「腎病飲食調配與商品推薦調換」+「AI 輔助」徽章**（取代原「配餐優化建議」純文字）。
- **四個分類資訊標籤按鈕** 改為設計圖的「icon 在上、文字在下」四宮格樣式，選中態白底 + 橘框；各標籤切換顯示對應內容（商品換掉 / 自拍成分 / 實體通路 / 輸入成分），切換邏輯沿用既有 `activeTab` state。
- **商品換掉（03）**：補上「🔍 診斷：早、中、晚三餐成分中…」說明文字。
- **自拍成分（04）**：改為設計圖的「置中相機虛線框」——圓形相機圖示 +「拍攝/上傳背面包裝成分圖」+「支持即時 OCR 辨識多寡項」。
  - **相機行為**：`<input capture="environment">` → 手機點擊直接開後鏡頭；電腦版自動回退為選檔上傳。選圖後接既有 `POST /api/instant-analyze` 流程（沿用，未改 API）。
- **實體通路（05）/ 輸入成分（06）**：版面沿用既有實作，已與設計圖一致。

### 詳細報告按鈕（06）
- 「點入詳細分析報告」由白底外框鈕改為 **深色實心鈕**（對照 06 圖）。

---

## 二、詳細報告 bottom-sheet modal（report-01~05）

`DetailedReportModal` 改版對照設計圖：
- **標題列**：深色星形圖示 + 中文標題「全域綜合飲食分析報告」+ 英文副標「GLOBAL DIETARY / COMPREHENSIVE REPORT」+ 圓形關閉鈕。
- **系統分析基礎宣告**：深色圓角卡 + 星形圖示，文案改為設計圖版本（「…透過以下 9 大客觀數據進行綜合交叉分析：」）。
- **「九大運算準則」分隔列** + 漸層分隔線。
- **九大項目**：每項為「深色編號圓圈 (1~9) + 粗體標題 + 斜體灰內容」。
  - **標題固定**（已存於 `DETAIL_REPORT_ITEMS`，本次把標題對齊設計圖文字、移除 ①②③ 前綴改用編號圓圈）：國際營養基準比對 / 水分攝取預估參照 / 專屬飲食限制對照 / 成分學理客觀標註 / 官方食安通報同步 / 潛在藥食關聯提示 / 動態熱量變數試算 / 換食過渡配比推估 / 日誌時序關聯比對。
  - **內容正式環境由 AI 生成**（`/api/diet-analysis` 的 `detailedReport`，未改後端結構）。
- **底部**：「AI SYNTHESIS REPORT V2.4」版本標記 + 深色膠囊「確認並關閉」鈕。

---

## 三、Mock 降級（前端，供驗證長相）

- 新增前端常數 `MOCK_DIET_ANALYSIS`（`DietAnalysisResult` 完整結構，內容取自設計圖各圖文字，含九大報告項）。
- 接點：`handleAiAnalyze` 的 catch 區 —— **AI 失敗時降級顯示 mock**，並保留提示「AI 分析暫時無法使用（…），以下為示意內容。」讓使用者知道看到的是示意資料。
- **正式環境主資料一律走 AI**（`/api/diet-analysis`）；mock 僅在 AI 失敗時呈現，不影響正常流程。
- 未改 `prisma/seed.ts`、未改後端 `route.ts`。

---

## 四、驗證

- `npx tsc --noEmit`：**通過，無型別錯誤**。
- `eslint src/app/diet/page.tsx`：剩 3 個 **既有** 錯誤（expert comment 的 `"` 未跳脫 @ L818、`SessionAccordionWithPlan` 內 effect 同步 setState @ L1457）——皆為先前既有、位於 4-4／既有區塊，**非本次新增**（已用 git diff 確認不在本次變更行）。

## 五、未做 / 待確認

- 後端 `/api/diet-analysis` prompt 與回傳結構未調整（既有結構已涵蓋本次 UI 所需欄位，無需改）。若日後要讓報告內容更貼近九大標題語氣，可再微調 prompt 的「欄位說明」。
- mock 為前端降級用；真機端對端（手機開相機、實際 AI 回傳長相）建議交 QA / frank 驗證。
