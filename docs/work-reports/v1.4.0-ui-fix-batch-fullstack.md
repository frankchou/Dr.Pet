# 工作報告：v1.4.0 UI/功能修復批次

**角色**：全端工程師（Fullstack Engineer）  
**日期**：2026-06-02  
**版本**：v1.4.0

---

## 執行摘要

本次修復批次解決了 20 個以上的 UI 錯誤與功能缺失，主要集中在分析頁、首頁健康模組、日誌頁功能完整性。

---

## 修改清單

### Bug 修復

| 項目 | 檔案 | 說明 |
|------|------|------|
| 分析頁 badge 邏輯 | `IngredientAnalysis.tsx` | 移除錯誤的 `!toxic&&!warning` 條件，改為 amber 軟色樣式 |
| ingredientText JSON 解析 | `ingredientAnalyzer.ts` | 支援解析含 markdown code block 的 JSON，修復部分產品只顯示 1 種成分 |
| 依產品 displayName 過濾 | `IngredientAnalysis.tsx` | 改用 `brand+name` 格式，修復有品牌產品過濾失效 |
| 寵物性別顯示 | `page.tsx` | `genderLabel()` 加入 `'公'` 為 male，修復顯示 ♀ 的 bug |
| 症狀標籤英文 | `page.tsx` | 補 `symptomTypeLabel()` import 並套用 |
| NaN 天 bug | `page.tsx` | 用 `birthdayMMDD` 替代直接解析 ISO birthday |
| 未來日程表節日 tab 空白 | `page.tsx` | 加入生日卡片邏輯 |
| 毛孩成長分析假資料 | `page.tsx` | 換成真實餐數 + 依年齡動態文字 |
| 美容記錄 error 不顯示 | `diary/page.tsx` | 加入 error state 顯示 |
| symptoms API 空 body crash | `api/symptoms/route.ts` | 加 try/catch 防護 `request.json()` |

### 新功能

| 功能 | 涉及檔案 | 說明 |
|------|---------|------|
| AI 整體評估加入營養表 | `IngredientAnalysis.tsx` | 載入 `/api/nutrition-ai`，顯示整體評估 + 各營養素卡片 + 整體建議 |
| 風險總覽營養安全風險區塊 | `IngredientAnalysis.tsx` | 從 nutritionByProduct 計算風險，加入 ACollapse |
| 健康指標完整功能 | schema + API + diary + home | HealthMetric model、CRUD API、日誌輸入 UI、首頁顯示 |
| BCS 1–9 對照表 | `diary/page.tsx` | 內嵌參考表格 |
| 美容新增大美容模式 | `diary/page.tsx` | 新增 `'full'` mode |
| 用藥看診多筆輸入 | `diary/page.tsx` | `+` 按鈕新增輸入框，`×` 刪除，批次儲存 |
| 日曆點選顯示紀錄 | `diary/page.tsx` + `api/diary-records` | DayDetail panel，GET API |
| AI 隨記解析儲存 | `diary/page.tsx` + `api/diary-parse` | Claude 解析 → 確認 → 批次儲存 |
| AI Google 商品搜尋 | `diary/page.tsx` + `api/products/web-search` | 雙軌搜尋，800ms debounce，AI 結果含風險分析 |
| 首頁 icon 對齊設計稿 | `page.tsx` | 飲食需知→三橢圓鏈，日常症狀→膠囊 |

---

## Schema 變更

```sql
-- migration: 20260602105349_add_health_metric
CREATE TABLE "HealthMetric" (
  "id" TEXT NOT NULL PRIMARY KEY,
  "petId" TEXT NOT NULL,
  "date" TEXT NOT NULL,
  "bodyScore" INTEGER,
  "vitality" TEXT,
  "waterIntake" TEXT,
  "notes" TEXT,
  "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" DATETIME NOT NULL,
  UNIQUE("petId", "date"),
  FOREIGN KEY ("petId") REFERENCES "Pet"("id") ON DELETE CASCADE
);
```

---

## 待辦（下一輪）

- 重要日程 / 未來日程表輸入 UI（UX spec 已完成，待 PM 確認）
- 美容記錄底層原因確認（API 已加防護，但原始觸發路徑待觀察）
