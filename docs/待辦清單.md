# Dr. Pet 待辦清單 TODO（分階段・細項版）

**來源：** `docs/系統盤點-2026-06-03.md` + 直接讀原始碼補充驗證
**建立日期：** 2026-06-03
**用途：** 把盤點文件中所有「尚缺／還沒做但該做」的項目，拆到檔案／API／步驟顆粒度，分階段追蹤，確保無遺漏。

> 勾選：`[ ]` 未開始 ｜ `[~]` 進行中 ｜ `[V]` 完成
> 優先度：P1 必修/影響使用 ｜ P2 功能完整性 ｜ P3 優化/加值

---

## 分階段規劃概覽

| Phase | 主題 | 優先 | 估時 | 排序理由 / 相依 | 狀態 |
|-------|------|------|------|----------------|------|
| **0** | 快速清理與補資料 | P1/P3 | 半天 | 零風險、最快見效；刪死碼 + 補 seed 讓多個空殼頁立刻有內容，後續開發也有資料可測 | ✅ 完成（正式庫 migration 留 deploy）|
| **1** | 共同飼主 Email 邀請（含權限修復、輪詢同步） | P1 | 2～3 天 | 已確認的當期主任務；牽動 25 支 API 權限補強 | 🔄 1-A/1-B 完成 |
| **2** | 首頁資料串接 | P1 | 2～3hr | 首頁第一印象；**相依 Phase 0** 的 seed 才有資料可顯示 | ⬜ |
| **3** | 既有 UI 功能補完 | P2 | 1～1.5 天 | 把 alert/disabled 的「假按鈕」變真功能 | ⬜ |
| **4** | AI 加值與空殼內容化 | P2 | 1～1.5 天 | 加值非必須、工程較重，核心穩定後再做 | ⬜ |
| **5** | 主動推播：食安警報 | P2 | 2～3 天 | 新功能；**相依 Phase 4-1** 快訊生成（需有食安警報來源） | ⬜ |

**排序原則：** 先清零風險快速見效 → 當期主任務 → 依主題分批補完。
**總估時：** 約 6～8 個工作天（不含 frank 的 Gmail 憑證前置）。

---

## Phase 0 — 快速清理與補資料（零風險、立刻見效）

### 0-1 ~~刪除死碼 `chat` 頁~~ 🚫 決議：**先保留，不刪**（frank 2026-06-04 拍板）
- 維持現狀（已加棄用註解），本次 Phase 0 不刪除。後續要刪再啟動。

### 0-2 刪除死碼 `SwitchPlanComingSoon()`（P3）✅ 完成
- [V] 在 `src/app/diet/page.tsx` 找到 `SwitchPlanComingSoon()` 定義與呼叫點（grep 確認零呼叫）
- [V] 確認換食計畫已改用 `DietSwitchPlan` 元件、此函式確實未被使用
- [V] 刪除函式與殘留 import（無殘留 import）

### 0-3 補 Demo Mock Data（P2）✅ 完成（全掛 demo 寵物「布丁」）
- [V] `SymptomEntry` ×5 → 修復 `/symptoms` 空白
- [V] `ProductReaction` ×6（遵守 `@@unique([petId,productId,date])`）→ 修復日誌頁產品評分
- [V] `ChatMessage` ×4 → 修復 `/nutritionist` 無歷史
- [V] `InstantAnalysis` ×3（safe/caution/danger）→ 修復 `/scan` 歷史空白
- [V] `NutritionAnalysis` ×1 → 修復 `/nutrition`
- [V] `NewsArticle` ×6（三分類各 2）→ 修復 `/news` 空殼
- [V] `AIInsight` ×1 → 修復 `/nutrition` 關聯分析
- [V] `WeeklyTask` ×4 → 修復 `/nutritionist` 計畫記錄
- [V] 執行 seed 成功（**僅對本機 dev.db**）、`tsc --noEmit` 通過。逐頁目視確認屬 QA 範疇（待 QA）

> ⚠️ **附帶發現（超出原範圍，全端已處理/標記）：**
> 1. `schema.prisma` 原本缺 3 個 model（`NewsArticle`/`DailyMealPlan`/`MealPlanItem`）——存在於 migration 與 DB、程式碼也引用，卻沒寫進 schema，導致 generate 出的 client 沒有 `newsArticle`（NewsArticle seed 與 `/api/news` 執行期本來就會壞）。已依 migration 補回，validate 通過。**需架構/技術文件覆核。**
> 2. 🔴 `.env` 的 `DATABASE_URL` 指向**正式站 Turso**；直接 `npx prisma db seed` 會把假資料寫進正式庫。全端**未對正式庫寫入**，改用本機 dev.db。**需 DevOps 處理 seed 防呆 + 正式庫 migration 落後問題。**（見下方新增的 0-4）

### 0-4 Seed 防呆 + 資料策略 ✅（正式庫 migration 留 deploy 時做）
- [V] seed 防呆：`seed.ts` 檢查 DATABASE_URL，非 `file:` 即中止（已實測擋下正式 URL）
- [V] demo/google 策略驗證：mock 全掛 demo-user/demo 寵物；全站無硬寫死 demo 寵物 id；`/api/pets` 依 userId 過濾；Google 新用戶首登乾淨、看不到 demo 資料（現況已正確，無需改碼）
- [ ] 🔴 正式站 Turso migration 落後（缺 HealthMetric 等表）→ **deploy 時由總指揮+DevOps 安全執行**（寫正式庫的對外動作，不在本次 commit）
- [V] 本機 dev.db 已由全端補跑 `add_diary_health_logs` migration（僅本機）

### 0-5 QA 發現的顯示斷鏈（既有 bug，seed 補了也看不到）
> 以下是 QA 靜態分析發現的既有問題：資料有進 DB，但頁面查詢邏輯撈不到 / 無消費端。屬既有架構 bug，非 seed 之錯，登記避免遺漏。
- [V] SymptomEntry 第 4 筆 `eye` → 改 UI 支援類型（`tear`）
- [V] **ProductReaction（產品評分/吃後感想）**（frank 最終決策）：屬 v1 功能、現版**不需要**。已從 `/diary` 移除 → 改放 **飲食頁 `/diet`** 但以旗標 `SHOW_PRODUCT_REACTIONS=false` **隱藏**；元件 `DailyReactionCard` 保留不刪、保持擴充彈性 → 列入 `docs/未來功能.md`，未來啟用把旗標改 true 即可（順帶保留的 `WeekCalendar` onDateSelect 週曆當日紀錄改善仍生效）
- [V] **AIInsight 補顯示端**（frank 決策）：`/api/analyze` 加 GET，新增 `CorrelationInsights` 掛到 `/nutrition`，含空狀態
- [V] **`/symptoms` 抓錯寵物**：改為 client component、用 localStorage `drpet_currentPetId`，對齊全站
- [V] **demo 產品 `nutritional_facts`**：seed 已補範例成分 → `/nutrition` 營養圖表有資料
- [V] **WeeklyTask 無消費端**（frank 決策）：**保留 seed 不刪**（保留彈性）；「歷史觀察計畫」顯示功能尚不存在 → 已列入 `docs/未來功能.md`，未來需要再做

### 0-6 修正手機版頭像選單 + 補「切換毛孩」入口（frank 回報的 bug）✅ 完成
- [V] `AppShell.tsx` 手機版左上頭像原為純 div（無功能）→ 改為點擊開啟 **ProfileMenu**，與桌面 Sidebar 行為一致
- [V] 選單含「切換毛孩 / 設定 / 登出」三區；**補上手機版原本完全缺失的「切換毛孩」入口**（用 localStorage `drpet_currentPetId` + storage event 與其他頁同步）
- [V] 修正先前誤把頭像直接導向 `/settings` 的錯誤（應為開選單，按「設定」才進設定頁）
- [V] 同一 header 的鈴鐺按鈕原無功能 → 接到「通知/快訊」頁 `/news`（frank 拍板）

---

## Phase 1 — 共同飼主 Email 邀請流程（當期主任務，P1）

### 1-A 前置：Gmail SMTP 寄信帳號 ⚠️【frank 要做的事，阻擋 1-B】
> 採 **Gmail SMTP + 應用程式密碼（App Password）**，不需 Google Cloud / OAuth / refresh token。
> 此方案本機與 Vercel 正式站皆可用；短期不申請網域也無需改 Resend。
> 寄件人顯示名由程式設為「無敏毛孩 PurePaw」，地址為下方 Gmail。
> （備註：原 Gmail API / OAuth 方案已棄用，太繁瑣且最終地址仍為 gmail。）

**步驟（只在 Gmail 帳號設定內操作，約 5 分鐘）**
- [V] 新辦一個專用 Gmail（建議，如 `purepaw.notify@gmail.com`，與私人信箱分開）
- [V] 該帳號開啟「兩步驟驗證」（App Password 的前提）
- [V] 產生一組「應用程式密碼」（16 碼）

**📦 frank 要提供給 Claude 的 2 樣東西（放進環境變數）**
- [V] `GMAIL_USER` — 寄件 Gmail 信箱
- [V] `GMAIL_APP_PASSWORD` — 上一步的 16 碼應用程式密碼

**環境變數設定位置**
- [V] 本機 `.env`（供 `npm run dev` 測試）
- [ ] 上 Vercel 時 → Settings → Environment Variables 同步填這 2 個值
- [ ] 確認 `NEXTAUTH_URL` 正式站指向 Vercel 網域（邀請信連結會用到，不能是 localhost）

> 🔐 安全提醒：App Password 等同寄信權限，勿提交進 git、勿貼公開頻道，只放環境變數。
> 📌 升級路線：未來若買網域、想要 noreply@自有網域，再換 Resend；屆時只改 `sendEmail()` 一個函式。

### 1-B 寄信能力（後端）✅ 完成
- [V] 新增 `src/lib/email.ts`，封裝 `sendEmail({ to, subject, html })`，內部用 nodemailer + Gmail SMTP，寄件顯示名「無敏毛孩 PurePaw」
- [V] 設計邀請信 HTML 模板（毛孩名、邀請人、`/invite/{token}` 連結、7 天效期說明、品牌色 #C4714A）
- [V] `src/app/api/pets/[id]/invitations/route.ts` POST：建立邀請後呼叫 `sendInviteEmail()`
- [V] 寄信失敗的錯誤處理（try/catch + log + 回傳 `emailSent` 旗標，不阻斷邀請流程）
- [V] `inviteUrl` 用 `NEXTAUTH_URL`（fallback `AUTH_URL`），非 localhost
- [V] SMTP 實測：寄測試信成功（messageId 已回，需收件匣目視確認）

### 1-C 共同飼主存取修復（必修 bug）
- [ ] `src/app/api/pets/route.ts` GET：查詢從「只撈 `userId===自己`」改為 `OR: [{ userId }, { members: { some: { userId } } }]`
- [ ] 確認接受邀請時 owner 端也有 `PetMember(role: owner)` 紀錄（否則 OR 查詢對 owner 失效）；缺則於建立寵物時補建 owner membership，並寫一次性 migration 補既有資料
- [ ] 補各「寵物資料 API」的 `requirePetAccess` 權限檢查（目前僅 invitations、members 有）：
  - [ ] `api/daily-health-log`
  - [ ] `api/diary-records`
  - [ ] `api/diary-parse`
  - [ ] `api/health-metrics`
  - [ ] `api/measurement-record`
  - [ ] `api/medication-record`
  - [ ] `api/grooming-record`
  - [ ] `api/symptoms`、`api/symptoms/[id]`
  - [ ] `api/pet-products`、`api/pet-products/[id]`
  - [ ] `api/reactions`
  - [ ] `api/usages`、`api/usages/[id]`
  - [ ] `api/documents`
  - [ ] `api/meal-plans`、`api/meal-plans/[id]/items`
  - [ ] `api/instant-analyze`、`api/instant-analyze/[id]`
  - [ ] `api/nutrition-ai`
  - [ ] `api/diet-analysis`
  - [ ] `api/analyze`、`api/analysis`
  - [ ] `api/recommend`
  - [ ] `api/chat`
  - [ ] `api/tasks`
  - [ ] `api/community/recs`、`api/community/trigger`
  - [ ] `api/pets/[id]`（單一寵物讀寫，含刪除權限限 owner）
  - [ ] 逐支檢視：讀取類允許 owner+co_owner；刪除毛孩等破壞性操作僅限 owner

### 1-D 共享資料即時同步（輪詢 + 聚焦重抓）
- [ ] 決定實作方式（SWR/React Query 的 `refreshInterval` + `revalidateOnFocus`，或自寫 `setInterval` + `visibilitychange`）
- [ ] 抽共用 hook（如 `usePolledFetch`），間隔 20–30 秒
- [ ] 套用到共享資料頁面：
  - [ ] 首頁 `src/app/page.tsx`
  - [ ] 日誌 `src/app/diary/page.tsx`
  - [ ] 飲食 `src/app/diet/page.tsx`
  - [ ] 寵物詳情 `src/app/pet/[id]/page.tsx`
  - [ ] 其餘顯示寵物資料的頁面視需要加上
- [ ] 確認背景分頁不浪費請求（聚焦才高頻、失焦降頻或停）

### 1-E QRCode 保留當備用
- [ ] 確認 `settings/page.tsx` 的 email 邀請為主要動線、QRCode 區塊調整為「或當面掃碼分享」次要位置
- [ ] 文案調整：說明已寄信 + QRCode 為備用

### 1-F 驗收測試
- [ ] A 帳號邀請 B 的 email → B 信箱真的收到信
- [ ] B 點信中連結 → 進登入頁 → Google 登入 → 跳法尊 → 同意 → 成為 co_owner
- [ ] B 首頁看得到被分享的毛孩（阿東）
- [ ] B 可新增/編輯該毛孩紀錄；A 端輪詢內自動看到更新
- [ ] B 不能存取「沒被分享的別人毛孩」（權限阻擋有效）
- [ ] 錯帳號登入、過期邀請、重複接受等邊界情境正確擋下

> 不在範圍（見 `docs/未來功能.md`）：撤銷/刪除邀請、邀請歷史分頁、通知發起者

---

## Phase 2 — 首頁資料串接（P1）

### 2-1 首頁「年度疫苗 / 體外驅蟲」
- [ ] `src/app/page.tsx` L383/388 移除 hardcoded `--`
- [ ] fetch MedicationRecord（疫苗/驅蟲類）；無對應 API 則確認/擴充 `api/medication-record` 查詢
- [ ] 顯示最近一次 + 下一次到期推估（依紀錄頻率）
- [ ] 無資料時顯示「尚未記錄」而非 `--`

### 2-2 首頁「未來日程表」醫療/美容 tab
- [ ] `src/app/page.tsx` L585 串 MedicationRecord / GroomingRecord 的未來排程
- [ ] 醫療 tab、美容 tab 各自顯示對應資料
- [ ] 真的無資料時才顯示「尚無日程記錄」

---

## Phase 3 — 既有 UI 功能補完（P2）

### 3-1 頭像上傳
- [ ] `src/app/settings/page.tsx` L392 移除 `disabled` 與「即將推出」title
- [ ] 串 `api/upload`，上傳後寫回 Pet/User 的 avatar 欄位
- [ ] 上傳中 loading、失敗提示、預覽

### 3-2 提醒機制（用藥/美容/Checklist）
> 三項共用「提醒」基礎，需先決定提醒怎麼送 → 建議先開小討論
- [ ] 決策：提醒形式（站內標記 / 推播 / email / 行事曆）
- [ ] 用藥 Modal「設定提醒」取代 `alert`（`MedicationModal.tsx` L321）
- [ ] 美容 Modal「設定提醒」取代 `alert`（`GroomingModal.tsx` L287）
- [ ] DailyChecklist 設定齒輪取代 `alert`（`DailyChecklist.tsx` L70）

### 3-3 SmartMemo 語音輸入
- [ ] `src/app/diary/page.tsx` L516 移除 `disabled`
- [ ] 串 Web Speech API（語音轉文字），含瀏覽器不支援的降級處理
- [ ] 錄音中狀態 UI、權限拒絕提示

---

## Phase 4 — AI 加值與空殼頁面內容化（P2）

### 4-1 快訊 AI 生成機制
- [ ] 設計 AI 定期生成快訊流程（`api/news/crawl` 已存在，確認其行為）
- [ ] 排程觸發（Vercel Cron 或手動觸發）寫入 NewsArticle
- [ ] `/news` 三分類顯示真實生成內容

### 4-2 症狀詳情頁 AI 建議
- [ ] `src/app/symptoms/[type]/page.tsx` 串 `api/chat`（或新 endpoint）
- [ ] 依症狀紀錄產生「可能原因 / 建議做法」，附 `VET_REFERENCE_SCOPE`
- [ ] 結果快取，避免每次重打 AI

### 4-3 日誌月摘要 AI 解讀
- [ ] `MonthHealthOverview` 統計數字下方加一段 AI 解讀
- [ ] 串 AI endpoint，依當月統計生成；無資料不顯示

---

## Phase 5 — 主動推播：食安警報（新功能，P2）

**目標：** 系統主動推播「快訊中的食安警報」到使用者裝置，即使沒開 App 也能即時收到（如某產品/成分被列為食安風險）。
**相依：** Phase 4-1 快訊生成機制（需先有「食安警報」分類的快訊來源）。
**前置決策（需先拍板，見下方問題）：** 推播技術選 Web Push（VAPID + Service Worker）或 Firebase Cloud Messaging（FCM）。

### 5-A 技術選型與前置
- [ ] 決策：Web Push（VAPID，Vercel 原生、無第三方）vs FCM（frank 熟悉 Firebase，但要引入 Firebase）
- [ ] （若 Web Push）產生 VAPID 金鑰對，`VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` 放環境變數
- [ ] iOS 限制確認：iOS 16.4+ 且須「加到主畫面」的 PWA 才能收 Web Push（需在 UX 說明）

### 5-B 訂閱基礎建設
- [ ] 新增 Service Worker（`public/sw.js` 或 next-pwa），處理 `push` / `notificationclick` 事件
- [ ] 前端：請求通知權限的 UX（適當時機提示，非一進站就要權限）
- [ ] 前端：取得 PushSubscription 並送後端儲存
- [ ] 新增 `PushSubscription` model（per user 的 endpoint/keys），及存/刪 API

### 5-C 食安警報觸發推播
- [ ] NewsArticle 需有「食安警報」分類/旗標（與 Phase 4-1 快訊生成對齊）
- [ ] 後端：當新的食安警報快訊產生時，對訂閱者發送 Web Push（標題、摘要、點擊導向該快訊）
- [ ] 防重複推播（同一則警報只推一次）；失效訂閱（410/404）自動清除
- [ ] 推播觸發點：排程（Vercel Cron）或快訊生成流程內觸發

### 5-D 設定與驗收
- [ ] 設定頁可開/關「食安警報推播」
- [ ] 驗收：產生一則食安警報 → 已訂閱裝置收到推播 → 點擊導向快訊；關閉後不再收到

---

## 進度總覽

| Phase | 主題 | 優先 | 估時 | 狀態 |
|-------|------|------|------|------|
| 0 | 快速清理與補資料 | P1/P3 | 半天 | ✅ 完成（0-1 保留；正式庫 migration 留 deploy 執行）|
| 1 | 共同飼主 Email 邀請（含權限修復、輪詢同步） | P1 | 2～3 天 | 🔄 1-A/1-B 完成，1-C~1-F 待做 |
| 2 | 首頁資料串接 | P1 | 2～3hr | ⬜ |
| 3 | 既有 UI 功能補完 | P2 | 1～1.5 天 | ⬜ |
| 4 | AI 加值與空殼內容化 | P2 | 1～1.5 天 | ⬜ |
| 5 | 主動推播：食安警報 | P2 | 2～3 天 | ⬜ |

---

## 附錄：本次盤點補充驗證的事實
- `requirePetAccess` 已存在於 `src/lib/petAccess.ts`，但目前僅 `invitations`、`members` 兩支 API 使用；其餘約 25 支寵物資料 API 皆未做權限檢查（Phase 1-C 全列）。
- seed.ts 已建立 9 個 model，缺 8 個（Phase 0-3 全列）。

*P2 後續（撤銷/刪除邀請、邀請歷史分頁、通知發起者）見 `docs/未來功能.md`。*
*完成項目請更新勾選與進度總覽。*
