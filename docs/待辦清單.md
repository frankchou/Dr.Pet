# Dr. Pet 待辦清單 TODO（分階段・細項版）

**來源：** `docs/系統盤點-2026-06-03.md` + 直接讀原始碼補充驗證
**建立日期：** 2026-06-03
**用途：** 把盤點文件中所有「尚缺／還沒做但該做」的項目，拆到檔案／API／步驟顆粒度，分階段追蹤，確保無遺漏。

> **追蹤改用兩個獨立維度：完成 + 驗證。**
>
> **完成（前方勾選框）：** `[ ]` 未開始 ｜ `[~]` 進行中 ｜ `[V]` 完成
> → 規則：**程式碼做完就打 `[V]`，不必等驗證通過**（驗證另以句尾標記表示）。
>
> **驗證（句尾標記）：** `✅` 驗證通過 ｜ `⏳` 待驗證 ｜ `➖` 不需驗證（frank 提供的參數/輸入/決策項）
>
> 每個細項格式範例：`- [V] 說明文字 — 驗證 ⏳`
>
> 優先度：P1 必修/影響使用 ｜ P2 功能完整性 ｜ P3 優化/加值

> ⚠️ **施工總則（工程師務必遵守）：**
> 1. 凡標有「**設計圖：`docs/features/ui-mockups/xxx.jpg`**」的項目，**實作前務必用 Read 工具開啟該圖、以圖為準、按圖施工**；待辦的文字僅輔助理解重點，**不取代看圖**。
> 2. 標示「mock / 正式環境由 AI…」的資料：現階段可用 mock 驗證長相，**正式環境須改由 AI 產生**，兩者都要做到。
> 3. 看不懂或圖文有出入時，先問 frank，不要自行臆測施工。

---

## 分階段規劃概覽

| Phase | 主題 | 優先 | 估時 | 排序理由 / 相依 | 狀態 | 驗證 |
|-------|------|------|------|----------------|------|------|
| **0** | 快速清理與補資料 | P1/P3 | 半天 | 零風險、最快見效；刪死碼 + 補 seed 讓多個空殼頁立刻有內容，後續開發也有資料可測 | ✅ 完成（正式庫 migration 留 deploy）| 部分✅/➖ |
| **1** | 共同飼主 Email 邀請（含權限修復、輪詢同步） | P1 | 2～3 天 | 已確認的當期主任務；牽動 25 支 API 權限補強 | 🔄 1-A~1-E 完成；1-F 待 frank 實機端對端 | 1-B~1-E ✅（review+QA邏輯層）；1-A ➖；1-F ⏳ |
| **2** | 首頁資料串接 + 日誌/飲食修正 | P1 | ~1 天 | 首頁串接 + frank 回報的日誌總覽/週曆/飲食 bug | 🔄 2-3/2-4 完成驗證；2-5 部分（前端硬化待做）；2-1/2-2 未開始 | 2-3/2-4 ✅；2-5 部分✅；2-1/2-2 ⏳ |
| **3** | 既有 UI 功能補完 + 導覽/版面調整 | P2 | 2～3 天 | 假按鈕變真功能；隱藏營養/諮詢頁、nav 重排、AI 諮詢移日誌浮動入口、毛孩頁快速入口 | ⬜ | ⏳ |
| **4** | AI 加值與空殼內容化 | P2 | 1～1.5 天 | 加值非必須、工程較重，核心穩定後再做 | ⬜ | ⏳ |
| **5** | 主動推播：食安警報 | P2 | 2～3 天 | 新功能；**相依 Phase 4-1** 快訊生成（需有食安警報來源） | ⬜ | ⏳ |

**排序原則：** 先清零風險快速見效 → 當期主任務 → 依主題分批補完。
**總估時：** 約 6～8 個工作天（不含 frank 的 Gmail 憑證前置）。

---

## Phase 0 — 快速清理與補資料（零風險、立刻見效）

### 0-1 ~~刪除死碼 `chat` 頁~~ 🚫 決議：**先保留，不刪**（frank 2026-06-04 拍板）
- 維持現狀（已加棄用註解），本次 Phase 0 不刪除。後續要刪再啟動。

### 0-2 刪除死碼 `SwitchPlanComingSoon()`（P3）✅ 完成
- [V] 在 `src/app/diet/page.tsx` 找到 `SwitchPlanComingSoon()` 定義與呼叫點（grep 確認零呼叫） — 驗證 ✅
- [V] 確認換食計畫已改用 `DietSwitchPlan` 元件、此函式確實未被使用 — 驗證 ✅
- [V] 刪除函式與殘留 import（無殘留 import） — 驗證 ✅

### 0-3 補 Demo Mock Data（P2）✅ 完成（全掛 demo 寵物「布丁」）
- [V] `SymptomEntry` ×5 → 修復 `/symptoms` 空白 — 驗證 ✅
- [V] `ProductReaction` ×6（遵守 `@@unique([petId,productId,date])`）→ 修復日誌頁產品評分 — 驗證 ✅
- [V] `ChatMessage` ×4 → 修復 `/nutritionist` 無歷史 — 驗證 ✅
- [V] `InstantAnalysis` ×3（safe/caution/danger）→ 修復 `/scan` 歷史空白 — 驗證 ✅
- [V] `NutritionAnalysis` ×1 → 修復 `/nutrition` — 驗證 ✅
- [V] `NewsArticle` ×6（三分類各 2）→ 修復 `/news` 空殼 — 驗證 ✅
- [V] `AIInsight` ×1 → 修復 `/nutrition` 關聯分析 — 驗證 ✅
- [V] `WeeklyTask` ×4 → 修復 `/nutritionist` 計畫記錄 — 驗證 ✅
- [V] 執行 seed 成功（**僅對本機 dev.db**）、`tsc --noEmit` 通過。逐頁目視確認屬 QA 範疇（待 QA） — 驗證 ✅

> ⚠️ **附帶發現（超出原範圍，全端已處理/標記）：**
> 1. `schema.prisma` 原本缺 3 個 model（`NewsArticle`/`DailyMealPlan`/`MealPlanItem`）——存在於 migration 與 DB、程式碼也引用，卻沒寫進 schema，導致 generate 出的 client 沒有 `newsArticle`（NewsArticle seed 與 `/api/news` 執行期本來就會壞）。已依 migration 補回，validate 通過。**需架構/技術文件覆核。**
> 2. 🔴 `.env` 的 `DATABASE_URL` 指向**正式站 Turso**；直接 `npx prisma db seed` 會把假資料寫進正式庫。全端**未對正式庫寫入**，改用本機 dev.db。**需 DevOps 處理 seed 防呆 + 正式庫 migration 落後問題。**（見下方新增的 0-4）

### 0-4 Seed 防呆 + 資料策略 ✅（正式庫 migration 留 deploy 時做）
- [V] seed 防呆：`seed.ts` 檢查 DATABASE_URL，非 `file:` 即中止（已實測擋下正式 URL） — 驗證 ✅
- [V] demo/google 策略驗證：mock 全掛 demo-user/demo 寵物；全站無硬寫死 demo 寵物 id；`/api/pets` 依 userId 過濾；Google 新用戶首登乾淨、看不到 demo 資料（現況已正確，無需改碼） — 驗證 ✅
- [ ] 🔴 正式站 Turso migration 落後（缺 HealthMetric 等表）→ **deploy 時由總指揮+DevOps 安全執行**（寫正式庫的對外動作，不在本次 commit） — 驗證 ➖
- [V] 本機 dev.db 已由全端補跑 `add_diary_health_logs` migration（僅本機） — 驗證 ✅

### 0-5 QA 發現的顯示斷鏈（既有 bug，seed 補了也看不到）
> 以下是 QA 靜態分析發現的既有問題：資料有進 DB，但頁面查詢邏輯撈不到 / 無消費端。屬既有架構 bug，非 seed 之錯，登記避免遺漏。
- [V] SymptomEntry 第 4 筆 `eye` → 改 UI 支援類型（`tear`） — 驗證 ✅
- [V] **ProductReaction（產品評分/吃後感想）**（frank 最終決策）：屬 v1 功能、現版**不需要**。已從 `/diary` 移除 → 改放 **飲食頁 `/diet`** 但以旗標 `SHOW_PRODUCT_REACTIONS=false` **隱藏**；元件 `DailyReactionCard` 保留不刪、保持擴充彈性 → 列入 `docs/未來功能.md`，未來啟用把旗標改 true 即可（順帶保留的 `WeekCalendar` onDateSelect 週曆當日紀錄改善仍生效） — 驗證 ✅
- [V] **AIInsight 補顯示端**（frank 決策）：`/api/analyze` 加 GET，新增 `CorrelationInsights` 掛到 `/nutrition`，含空狀態 — 驗證 ✅
- [V] **`/symptoms` 抓錯寵物**：改為 client component、用 localStorage `drpet_currentPetId`，對齊全站 — 驗證 ✅
- [V] **demo 產品 `nutritional_facts`**：seed 已補範例成分 → `/nutrition` 營養圖表有資料 — 驗證 ✅
- [V] **WeeklyTask 無消費端**（frank 決策）：**保留 seed 不刪**（保留彈性）；「歷史觀察計畫」顯示功能尚不存在 → 已列入 `docs/未來功能.md`，未來需要再做 — 驗證 ✅

### 0-6 修正手機版頭像選單 + 補「切換毛孩」入口（frank 回報的 bug）✅ 完成
- [V] `AppShell.tsx` 手機版左上頭像原為純 div（無功能）→ 改為點擊開啟 **ProfileMenu**，與桌面 Sidebar 行為一致 — 驗證 ✅
- [V] 選單含「切換毛孩 / 設定 / 登出」三區；**補上手機版原本完全缺失的「切換毛孩」入口**（用 localStorage `drpet_currentPetId` + storage event 與其他頁同步） — 驗證 ✅
- [V] 修正先前誤把頭像直接導向 `/settings` 的錯誤（應為開選單，按「設定」才進設定頁） — 驗證 ✅
- [V] 同一 header 的鈴鐺按鈕原無功能 → 接到「通知/快訊」頁 `/news`（frank 拍板） — 驗證 ✅

---

## Phase 1 — 共同飼主 Email 邀請流程（當期主任務，P1）

### 1-A 前置：Gmail SMTP 寄信帳號 ⚠️【frank 要做的事，阻擋 1-B】
> 採 **Gmail SMTP + 應用程式密碼（App Password）**，不需 Google Cloud / OAuth / refresh token。
> 此方案本機與 Vercel 正式站皆可用；短期不申請網域也無需改 Resend。
> 寄件人顯示名由程式設為「無敏毛孩 PurePaw」，地址為下方 Gmail。
> （備註：原 Gmail API / OAuth 方案已棄用，太繁瑣且最終地址仍為 gmail。）

**步驟（只在 Gmail 帳號設定內操作，約 5 分鐘）**
- [V] 新辦一個專用 Gmail（建議，如 `purepaw.notify@gmail.com`，與私人信箱分開） — 驗證 ➖
- [V] 該帳號開啟「兩步驟驗證」（App Password 的前提） — 驗證 ➖
- [V] 產生一組「應用程式密碼」（16 碼） — 驗證 ➖

**📦 frank 要提供給 Claude 的 2 樣東西（放進環境變數）**
- [V] `GMAIL_USER` — 寄件 Gmail 信箱 — 驗證 ➖
- [V] `GMAIL_APP_PASSWORD` — 上一步的 16 碼應用程式密碼 — 驗證 ➖

**環境變數設定位置**
- [V] 本機 `.env`（供 `npm run dev` 測試） — 驗證 ➖
- [ ] 上 Vercel 時 → Settings → Environment Variables 同步填這 2 個值 — 驗證 ➖
- [ ] 確認 `NEXTAUTH_URL` 正式站指向 Vercel 網域（邀請信連結會用到，不能是 localhost） — 驗證 ➖

> 🔐 安全提醒：App Password 等同寄信權限，勿提交進 git、勿貼公開頻道，只放環境變數。
> 📌 升級路線：未來若買網域、想要 noreply@自有網域，再換 Resend；屆時只改 `sendEmail()` 一個函式。

### 1-B 寄信能力（後端）✅ 完成
- [V] 新增 `src/lib/email.ts`，封裝 `sendEmail({ to, subject, html })`，內部用 nodemailer + Gmail SMTP，寄件顯示名「無敏毛孩 PurePaw」 — 驗證 ✅
- [V] 設計邀請信 HTML 模板（毛孩名、邀請人、`/invite/{token}` 連結、7 天效期說明、品牌色 #C4714A） — 驗證 ✅
- [V] `src/app/api/pets/[id]/invitations/route.ts` POST：建立邀請後呼叫 `sendInviteEmail()` — 驗證 ✅
- [V] 寄信失敗的錯誤處理（try/catch + log + 回傳 `emailSent` 旗標，不阻斷邀請流程） — 驗證 ✅
- [V] `inviteUrl` 用 `NEXTAUTH_URL`（fallback `AUTH_URL`），非 localhost — 驗證 ✅
- [V] SMTP 實測：寄測試信成功（messageId 已回，需收件匣目視確認） — 驗證 ✅

### 1-C 共同飼主存取修復（必修 bug）
- [V] `src/app/api/pets/route.ts` GET：查詢從「只撈 `userId===自己`」改為 `OR: [{ userId }, { members: { some: { userId } } }]` — 驗證 ✅
- [V] 確認接受邀請時 owner 端也有 `PetMember(role: owner)` 紀錄（否則 OR 查詢對 owner 失效）；缺則於建立寵物時補建 owner membership，並寫一次性 migration 補既有資料 — 驗證 ✅
- [V] 補各「寵物資料 API」的 `requirePetAccess` 權限檢查（目前僅 invitations、members 有）：
  - [V] `api/daily-health-log` — 驗證 ✅
  - [V] `api/diary-records` — 驗證 ✅
  - [V] `api/diary-parse` — 驗證 ✅
  - [V] `api/health-metrics` — 驗證 ✅
  - [V] `api/measurement-record` — 驗證 ✅
  - [V] `api/medication-record` — 驗證 ✅
  - [V] `api/grooming-record` — 驗證 ✅
  - [V] `api/symptoms`、`api/symptoms/[id]` — 驗證 ✅
  - [V] `api/pet-products`、`api/pet-products/[id]` — 驗證 ✅
  - [V] `api/reactions` — 驗證 ✅
  - [V] `api/usages`、`api/usages/[id]` — 驗證 ✅
  - [V] `api/documents` — 驗證 ✅
  - [V] `api/meal-plans`、`api/meal-plans/[id]/items` — 驗證 ✅
  - [V] `api/instant-analyze`、`api/instant-analyze/[id]` — 驗證 ✅
  - [V] `api/nutrition-ai` — 驗證 ✅
  - [V] `api/diet-analysis` — 驗證 ✅
  - [V] `api/analyze`、`api/analysis` — 驗證 ✅
  - [V] `api/recommend` — 驗證 ✅
  - [V] `api/chat` — 驗證 ✅
  - [V] `api/tasks` — 驗證 ✅
  - [V] `api/community/recs`、`api/community/trigger` — 驗證 ✅
  - [V] `api/pets/[id]`（單一寵物讀寫，含刪除權限限 owner） — 驗證 ✅
  - [V] 逐支檢視：讀取類允許 owner+co_owner；刪除毛孩等破壞性操作僅限 owner — 驗證 ✅

### 1-D 共享資料即時同步（輪詢 + 聚焦重抓）
- [V] 決定實作方式（SWR/React Query 的 `refreshInterval` + `revalidateOnFocus`，或自寫 `setInterval` + `visibilitychange`） — 驗證 ✅
- [V] 抽共用 hook（如 `usePolledFetch`），間隔 20–30 秒 — 驗證 ✅
- [V] 套用到共享資料頁面：
  - [V] 首頁 `src/app/page.tsx` — 驗證 ✅
  - [V] 日誌 `src/app/diary/page.tsx` — 驗證 ✅
  - [V] 飲食 `src/app/diet/page.tsx` — 驗證 ✅
  - [V] 寵物詳情 `src/app/pet/[id]/page.tsx` — 驗證 ✅
  - [V] 其餘顯示寵物資料的頁面視需要加上 — 驗證 ✅
- [V] 確認背景分頁不浪費請求（聚焦才高頻、失焦降頻或停） — 驗證 ✅

### 1-E QRCode 保留當備用
- [V] 確認 `settings/page.tsx` 的 email 邀請為主要動線、QRCode 區塊調整為「或當面掃碼分享」次要位置 — 驗證 ✅
- [V] 文案調整：說明已寄信 + QRCode 為備用 — 驗證 ✅

### 1-F 驗收測試
- [ ] A 帳號邀請 B 的 email → B 信箱真的收到信 — 驗證 ⏳
- [ ] B 點信中連結 → 進登入頁 → Google 登入 → 跳法尊 → 同意 → 成為 co_owner — 驗證 ⏳
- [ ] B 首頁看得到被分享的毛孩（阿東） — 驗證 ⏳
- [ ] B 可新增/編輯該毛孩紀錄；A 端輪詢內自動看到更新 — 驗證 ⏳
- [ ] B 不能存取「沒被分享的別人毛孩」（權限阻擋有效） — 驗證 ⏳
- [ ] 錯帳號登入、過期邀請、重複接受等邊界情境正確擋下 — 驗證 ⏳

> 不在範圍（見 `docs/未來功能.md`）：撤銷/刪除邀請、邀請歷史分頁、通知發起者

---

## Phase 2 — 首頁資料串接 + 日誌/飲食修正（P1）

> 註：本 Phase 原為「首頁資料串接」(2-1/2-2)，後併入 frank 回報的日誌/飲食 bug（2-3 月曆總覽、2-4 週曆互動、2-5 飲食頁）。2-3/2-4/2-5 已實作，**尚未 commit、待 frank 實機驗證**後收尾。

### 2-1 首頁「年度疫苗 / 體外驅蟲」
- [ ] `src/app/page.tsx` L383/388 移除 hardcoded `--` — 驗證 ⏳
- [ ] fetch MedicationRecord（疫苗/驅蟲類）；無對應 API 則確認/擴充 `api/medication-record` 查詢 — 驗證 ⏳
- [ ] 顯示最近一次 + 下一次到期推估（依紀錄頻率） — 驗證 ⏳
- [ ] 無資料時顯示「尚未記錄」而非 `--` — 驗證 ⏳

### 2-2 首頁「未來日程表」醫療/美容 tab
- [ ] `src/app/page.tsx` L585 串 MedicationRecord / GroomingRecord 的未來排程 — 驗證 ⏳
- [ ] 醫療 tab、美容 tab 各自顯示對應資料 — 驗證 ⏳
- [ ] 真的無資料時才顯示「尚無日程記錄」 — 驗證 ⏳

### 2-3 日誌月曆總覽修正（frank 回報，P1）
> 診斷結論：主要是串資料/邏輯 bug，非 mock 問題。核心修法：總覽**統一以健康日誌 DailyHealthLog 為基準**，並為各觀察欄位定義「正常值白名單」。決策：先修 3 個 bug + 加白話摘要，版面重新設計留待之後（見未來功能）。
- [V] **bug1 情緒誤判**：`MonthHealthOverview.tsx:166-171` 只要 mood 有值就算異常 → 改為「非正常值才算異常」（為 mood 等多選觀察欄位定義正常值白名單，如「平靜放鬆」） — 驗證 ✅
- [V] **bug2 健康良好 -4**：`L243` `recordedCount − abnormalDays` 兩者來自不同表（飲食 vs 健康日誌）→ 改為全部用 DailyHealthLog 計算（良好 = 有日誌天數 − 異常天數） — 驗證 ✅
- [V] **bug3 已紀錄 0/30**：`L138` 改用「健康日誌有紀錄的天數」為基準（frank 決策），不再用 ProductUsage — 驗證 ✅
- [V] **加一行白話摘要**：總覽最上方加一句總結（例：「本月 18 天有紀錄，14 天狀況良好，4 天有軟便」），並修正各統計標籤語意 — 驗證 ✅
- [V] **mock 小修**：seed 的 mood「活潑好動」不在 MoodCard 選項中 → 統一（補進選項或改 seed 值） — 驗證 ✅

### 2-5 飲食頁「繼續添加項目」卡在「建立配餐計畫中…」（frank 回報，P1）
> 根因：`model DailyMealPlan` 在 v1.6.0（commit f595dad 日誌改版）被誤從 schema.prisma 移除 → prisma client 沒有 `dailyMealPlan` → `/api/meal-plans` 建立計畫 500 → 前端 `ensurePlan()` 回 null → 無限轉圈。**後端已於 Phase 0（commit 3292e4e）補回 model，本機 upsert 實測 OK。**
- [V] schema 補回 `DailyMealPlan`（已隨 3292e4e 完成） — 驗證 ✅
- [V] 確認部署/開發環境 `npx prisma generate` + 重啟，飲食頁恢復新增項目（已實測 API 201） — 驗證 ✅
- [ ] 正式站 Turso 需確認有此表（併入 0-4 正式庫 migration 一起處理） — 驗證 ➖
- [ ] **前端硬化**：`ensurePlan()` 失敗時不要無限轉圈 → 顯示錯誤訊息 + 可重試（`diet/page.tsx` ensurePlan / SessionAccordionWithPlan） — 驗證 ⏳

### 2-4 日誌月曆/週曆互動修正（frank 回報，P1，與 2-3 同批）
> 診斷結論：日曆「點點」與「已紀錄天數」資料來源不統一；週曆寫死本週、點日期不連動、且有一塊重複的空區塊。
- [V] **點點資料源統一**：日期下方「有紀錄圓點」目前只看 ProductUsage（`page.tsx:1250-1257`），demo 無飲食紀錄就永遠不顯示 → 改為「該天有任何紀錄就顯示點」（涵蓋 DailyHealthLog + ProductUsage + 症狀/用藥/美容/量測等），月曆與週曆一致 — 驗證 ✅
- [V] **已紀錄天數語意對齊**：上方「已紀錄 X 天」要與點點同義（有紀錄的不重複天數，例：6/1~6/4 各有紀錄=4 天）；與 2-3 的基準統一 — 驗證 ✅
- [V] **週曆點日期連動**：點週曆某天，下方當日紀錄要跟著換（`WeekCalendar` 改受控、與頁面 `selectedDate` 同步，確認 `HealthLogSection` 吃到正確 date；排查時區/內部 state 衝突） — 驗證 ✅
- [V] **隱藏/修正空的「(日期)的紀錄」區塊**：週曆點日期會冒出一塊看似要顯示總結卻空白的區塊（疑似 `WeekCalendar` 內建 `DayDetail`）→ 與下方連動的紀錄重複；決策：移除/隱藏這塊（frank：不顯示總結就先隱藏） — 驗證 ✅
- [V] **週曆可切換週**：月曆能換月，週曆要能上一週/下一週（`page.tsx:411-447` 寫死本週 → 加 anchor state + 上/下週導覽，可參考 `DiaryCalendar.tsx` 既有 WeekView） — 驗證 ✅
- [V] **mock 每日資料變化**（frank 回報「每天長一樣」）：seed 原本最近 6 天都對應「正常」樣態 → 改為 6 種樣態輪替，6/1~6/4 各自不同（已查 DB 確認）；並修正既有不合法值（`軟便`→`便便偏軟`、`帶黏液` 移到 stoolDetails）。已重 seed dev.db — 驗證 ✅（DB 已確認；畫面待 frank 重整點擊）

### 2-6 手機版 bottom-sheet modal 無法捲動、完成按鈕被擋（frank 回報，P1 bug）
- [ ] 週曆「用藥看診 / 洗澡美容 / 量測紀錄」點開的 bottom-sheet modal，在**手機版無法捲動** → 最下面「完成紀錄」按鈕被擋住，無法送出 — 驗證 ⏳
- [ ] 修 modal 的 scroll（高度/overflow/safe-area），確保手機版可捲到底並按到完成；桌面版不受影響 — 驗證 ⏳

### 2-7 飲食頁早/午/晚餐有項目時的展開/收合顯示（frank 回報）
> 設計圖：展開 `docs/features/ui-mockups/diet-meal-session-expanded.jpg`；收合 `docs/features/ui-mockups/diet-meal-session-collapsed.jpg`
- [ ] 有項目時，**展開**狀態（見 expanded 圖）：逐項顯示產品名 + 標籤 + 數量/單位 + 預估克數 + 「繼續添加項目」 — 驗證 ⏳
- [ ] **收合**狀態（見 collapsed 圖）：精簡列出「產品名 (數量 單位)」；未添加則顯示「尚未添加配餐項目」 — 驗證 ⏳

### 2-8 飲食頁「換食計畫」畫面對照設計圖（frank 提出）
> 現況：換食計畫（`DietSwitchPlan` 元件）已實作；需對照設計圖確認長相、缺資料則補 mock。
> 設計圖：`docs/features/ui-mockups/diet-switch-plan-01.jpg`（換食計畫入口點下去後的畫面）
- [ ] 對照設計圖確認畫面一致；**不一致則改成與設計圖一致** — 驗證 ⏳
- [ ] 畫面區塊：當前測試商品（測試中第 N/14 天、配方名、替換目標、標籤）／7 天換食排程建議（今日建議比例 新:舊 + 進度條）／身體特徵監控（大便分數、抓癢頻率、趨勢）／底部「晉升日常飲食」「淘汰並更換」兩鈕 — 驗證 ⏳
- [ ] 缺 mock_data 則補（測試商品、排程比例、身體特徵監控數據）以供驗證；正式環境接真實資料 — 驗證 ⏳

---

## Phase 3 — 既有 UI 功能補完（P2）

### 3-1 頭像上傳
- [ ] `src/app/settings/page.tsx` L392 移除 `disabled` 與「即將推出」title — 驗證 ⏳
- [ ] 串 `api/upload`，上傳後寫回 Pet/User 的 avatar 欄位 — 驗證 ⏳
- [ ] 上傳中 loading、失敗提示、預覽 — 驗證 ⏳

### 3-2 提醒機制（用藥/美容/Checklist）
> 三項共用「提醒」基礎，需先決定提醒怎麼送 → 建議先開小討論
- [ ] 決策：提醒形式（站內標記 / 推播 / email / 行事曆） — 驗證 ⏳
- [ ] 用藥 Modal「設定提醒」取代 `alert`（`MedicationModal.tsx` L321） — 驗證 ⏳
- [ ] 美容 Modal「設定提醒」取代 `alert`（`GroomingModal.tsx` L287） — 驗證 ⏳
- [ ] DailyChecklist 設定齒輪取代 `alert`（`DailyChecklist.tsx` L70） — 驗證 ⏳

### 3-3 SmartMemo 語音輸入
- [ ] `src/app/diary/page.tsx` L516 移除 `disabled` — 驗證 ⏳
- [ ] 串 Web Speech API（語音轉文字），含瀏覽器不支援的降級處理 — 驗證 ⏳
- [ ] 錄音中狀態 UI、權限拒絕提示 — 驗證 ⏳

### 3-4 隱藏「營養分析」「AI 諮詢」頁 + nav 重排（frank 提出）
> 共通備註：以下「modal」均指像日誌週曆「用藥看診」那種**從下方滑出的 bottom sheet**。
- [ ] 隱藏「營養分析」頁（電腦左側 nav / 手機下方「營養」）→ 先隱藏、日後可開（保留程式碼，nav 移除入口） — 驗證 ⏳
- [ ] 隱藏「AI 諮詢」頁（`/nutritionist`）→ 先隱藏、日後可開（保留程式碼，nav 移除入口；功能改掛日誌頁見 3-5） — 驗證 ⏳
- [ ] nav 重排為 **毛孩｜飲食｜照相｜日誌｜快訊**（`Sidebar.tsx` 電腦版 + `BottomNav.tsx` 手機版同步） — 驗證 ⏳

### 3-5 AI 諮詢移為日誌頁浮動入口 + modal（frank 提出，相依 3-4）
- [ ] 在日誌頁加一個**置底+置右、隨頁面浮動的圓形 icon** 入口（fixed 定位、滑動時固定） — 驗證 ⏳
- [ ] 點 icon → 開 bottom-sheet modal，內容為 AI 諮詢功能（複製/重用 `/nutritionist` 的對話功能） — 驗證 ⏳

### 3-6 毛孩頁「快速入口」加 3 個（frank 提出）
> 設計圖：`docs/features/ui-mockups/home-quick-entries.jpg`
- [ ] 新增三個入口：**日查觀察表**（DAILY OBSERVATION FORM）、**就醫記錄表**（MEDICAL VISIT RECORD）、**緊急協助 SOS**（EMERGENCY ASSISTANCE，紅底強調） — 驗證 ⏳
- [ ] 確認三者各自的目的地/行為（日查觀察→日誌觀察、就醫記錄→醫療紀錄、緊急協助→待定義 SOS 行為） — 驗證 ⏳

### 3-7 每日任務管理 modal（「每日紀錄項目」的「設定」連結，frank 提出）
> 即系統盤點裡 `DailyChecklist.tsx` L70「設定功能即將推出」alert 的完整定義；取代 3-2 的 Checklist 設定 alert。
> 註：UI 是「每日紀錄項目」區塊**右上角的「設定」文字連結**（非齒輪 icon）。
> 設計圖：入口 `docs/features/ui-mockups/diary-daily-record-block.jpg`；modal `diary-daily-task-modal-1.jpg`、`diary-daily-task-modal-2.jpg`
- [ ] 日誌週曆「每日紀錄項目」區塊右上角的**「設定」文字連結**點下去 → 開 bottom-sheet modal「每日任務管理」 — 驗證 ⏳
- [ ] modal 內每個任務：開/關 toggle + 備註欄（如「刷牙清潔／使用紗布刷牙加牙粉」）；支援「新增自訂任務」（如點眼藥水）、「全部取消」、底部「完成設定」 — 驗證 ⏳
- [ ] 連動「每日記錄項目」區塊：新增任務**預設開啟**；**關閉的任務在該區塊隱藏**；該區塊只顯示目前開啟的任務（開幾個顯示幾個） — 驗證 ⏳
- [ ] 任務清單 + 開關 + 備註需持久化（評估用既有 `useRecordParams` / `dailyChecklist` 欄位，或新資料結構）；確認與既有 DailyChecklist 完成勾選相容 — 驗證 ⏳

### 3-8 日誌月曆縮小 + 月/週切換移到右上角（frank 回報）
- [ ] 月曆整體**縮小**：建議比例**約 0.85（cell 高度/字級/間距同步縮約 15%）**，frank 可再微調 — 驗證 ⏳
- [ ] 月/週切換**移到月曆右上角**（設計圖 `docs/features/ui-mockups/diary-calendar-toggle.jpg`）：月曆時顯示「週檢視」、週曆時顯示「月檢視」，點擊切換 — 驗證 ⏳
- [ ] **隱藏**日誌頁最上面那排月曆/週曆 tab；切換後既有功能（圓點、連動、切月/切週）全部不變 — 驗證 ⏳

### 3-9 週曆健康區塊右上角裝飾 icon 改為不可點（frank 回報，bug）
- [ ] 週曆紀錄「食慾咀嚼」～「生殖與分泌」各區塊右上角的 icon 是**純裝飾**（設計稿就是圖示，有些是相機圖案）→ 目前可點且有功能，要**移除點擊功能**、保留圖示外觀 — 驗證 ⏳

### 3-10 毛孩頁「健康檔案概覽」隱藏活力指數/水分攝取（frank 提出）
- [ ] 毛孩頁「健康檔案概覽」中的**活力指數**與**水分攝取**先隱藏（保留程式碼，日後可開）；同步登記到 `docs/隱藏與保留項目.md` — 驗證 ⏳

---

## Phase 4 — AI 加值與空殼頁面內容化（P2）

### 4-1 快訊 AI 生成機制
- [ ] 設計 AI 定期生成快訊流程（`api/news/crawl` 已存在，確認其行為） — 驗證 ⏳
- [ ] 排程觸發（Vercel Cron 或手動觸發）寫入 NewsArticle — 驗證 ⏳
- [ ] `/news` 三分類顯示真實生成內容 — 驗證 ⏳

### 4-2 症狀詳情頁 AI 建議
- [ ] `src/app/symptoms/[type]/page.tsx` 串 `api/chat`（或新 endpoint） — 驗證 ⏳
- [ ] 依症狀紀錄產生「可能原因 / 建議做法」，附 `VET_REFERENCE_SCOPE` — 驗證 ⏳
- [ ] 結果快取，避免每次重打 AI — 驗證 ⏳

### 4-3 日誌月摘要 AI 解讀
- [ ] `MonthHealthOverview` 統計數字下方加一段 AI 解讀 — 驗證 ⏳
- [ ] 串 AI endpoint，依當月統計生成；無資料不顯示 — 驗證 ⏳

### 4-4 飲食頁「添加項目」改 bottom-sheet modal + AI 產品搜尋選取（frank 提出，含原 item 5）
> 現況：飲食頁「新增項目」點下去是**下方 inline 展開**自由文字框，無法檢核對錯、易打錯 → 之後 AI 分析不準。
> 目標：(1) 改成點擊**開 bottom-sheet modal**（從下方滑出，樣式同日誌「用藥看診」）；(2) modal 內為 AI 輔助搜尋選取（版面如下方「modal 版面描述」）。
>
> **設計圖（以圖為準）：** `docs/features/ui-mockups/diet-add-item-ai-search.jpg`（modal 整體 + 卡片收合態）；
> 展開細節：`diet-add-item-detail-info-1.jpg`、`diet-add-item-detail-info-2.jpg`。
>
> **modal 版面描述（文字備援，細節以上述圖為準）：**
> - 頂部：**搜尋輸入框**（放大鏡圖示 + placeholder，右側可放語音 mic 圖示），打字即觸發 AI 搜尋。
> - 輸入框下：**分類篩選 chips**（全部商品 / 飼料 / 保健品 / 鮮食…）。
> - 標題列：「**AI 智能搜尋結果 (N)**」。
> - **結果卡片**（收合態，見 `diet-add-item-ai-search.jpg`）：產品分類標籤、「已加入」徽章、右上「＋」加入鈕、產品名稱、品牌+規格、可展開的「完整成分資訊（與包裝一致）」摘要、驗證標章（ISO 22000/AAFCO/FEDIAF）、AI 驗證數據出處 + 更新日期。
> - 底部：找不到時可「自訂新增」備援。
>
> **「完整成分資訊」展開／收合（frank 補充，見 detail-info 兩張圖）：**
> - **收合**：只顯示 `diet-add-item-ai-search.jpg` 那樣的基本成分摘要。
> - **展開**：顯示 detail-info-1/2 的完整細節——完整成分、營養添加物(每公斤)、營養成分表(蛋白/脂肪/鈣磷…+熱量)、商品規格(適用年齡/產地/功能/穀類/口味/軟硬度/顆粒)、製造與代理資訊、每日建議餵食量(體重→餵食量表)、錯誤回報。
> - **資料來源**：detail-info 圖中的資料**現階段可當 mock_data**；**正式環境必須由 AI 在搜尋產品時一併抓取該產品的詳細資料**填入展開內容（不是寫死）。
- [ ] 「添加項目」改為開 bottom-sheet modal（取代現有 inline 展開的 AddItemForm） — 驗證 ⏳
- [ ] modal 內依設計圖 `diet-add-item-ai-search.jpg` 實作：搜尋框 + 分類 chips + AI 結果卡片 + 加入鈕；保留「找不到時可自訂」備援 — 驗證 ⏳
- [ ] 卡片「完整成分資訊」可展開：收合如 ai-search 圖、展開如 detail-info-1/2 圖（成分/營養添加物/營養成分表/商品規格/製造代理/每日建議餵食量/錯誤回報） — 驗證 ⏳
- [ ] **搜尋結果產品有風險時的呈現**（設計圖 `diet-add-item-ai-search-risk-1.jpg`、`-risk-2.jpg`）：產品卡需依風險顯示警示，有兩種樣式：
  - **風險樣式 A — 產品風險（risk-1 圖）**：分類標籤旁加紅色警示 chip「⚠️ 建議謹慎使用」；卡片內多一塊紅色 **「⚠️ 風險說明」** 段落，文字描述具體風險（例：「該品牌旗下其他批次曾於 2024 抽驗發現重金屬超標，本款雖有檢驗報告但品牌合規記錄存在瑕疵，建議購買前再核對最新批次」）；數據出處指向風險來源（例：食藥署不合格紀錄、原廠回覆函）+ 數據更新日期。
  - **風險樣式 B — 品牌警示（risk-2 圖）**：產品名稱上方加一條紅色橫幅 **「⚠️ {品牌}（品牌警示：多項產品未通過核可）」**；產品本身仍可同時顯示正向驗證標章（如 IFOS 五星、SGS 重金屬檢驗）——即「品牌有疑慮但本品有檢驗」並陳。
  - **無風險**：照原 `diet-add-item-ai-search.jpg` 正常顯示（不出現警示 chip / 風險說明 / 品牌橫幅）。
  - **資料來源**：圖中風險文字現階段可當 mock；**正式環境由 AI 綜合「該毛孩健康狀態 + 主要問題 + 過敏原 + 飲食禁忌」與「該產品成分 / 品牌合規 / 食安通報紀錄」分析，判定此產品是否對這隻毛孩有害／需謹慎，再決定要不要顯示哪種風險樣式與文字**。 — 驗證 ⏳
- [ ] 串接產品搜尋（可用既有 `/api/products/web-search` AI 找產品 / `/api/products/lookup`）；**正式環境 AI 需一併抓回產品詳細資料供展開顯示**（detail-info 內容），現階段可先用 mock；加 debounce、loading、空結果處理 — 驗證 ⏳
- [ ] 選取後存標準化產品（productId 綁定 + 詳細資料），讓營養/成分分析能對應到真實產品資料 — 驗證 ⏳

### 4-5 飲食頁「AI 智能分析配餐」結果畫面對照設計圖（frank 提出）
> 現況：此功能（`/api/diet-analysis`、`DietAnalysisResult.detailedReport`）**已實作，但因缺 mock_data 從未驗證過長相**。
> 設計圖（已上傳）：
> - 主畫面連貫圖 `docs/features/ui-mockups/diet-ai-analysis-01.jpg ~ -06.jpg`（由上到下標示各資訊位置與內容）
> - 詳細報告 modal `diet-ai-analysis-report-01.jpg ~ -05.jpg`
> 圖中資訊現階段可當 mock；**正式環境須由 AI 分析產生**。
- [ ] 補 mock_data（`DietAnalysisResult` / `detailedReport`）讓「AI 智能分析配餐」結果畫面能呈現以供驗證 — 驗證 ⏳
- [ ] 逐區對照主畫面設計圖（01~06）：確認實際畫面與設計一致；**不一致則改成與設計圖一致** — 驗證 ⏳
- [ ] **四個分類資訊標籤按鈕**（對應 03~06）：各標籤切換顯示對應內容 — 驗證 ⏳
- [ ] **04 的「自拍成分」相機**：點相機 → 手機開相機 / 電腦版上傳圖片（接既有 instant-analyze 或對應流程） — 驗證 ⏳
- [ ] **06 的「點入詳細分析報告」按鈕** → 開 bottom-sheet modal，內容依 `diet-ai-analysis-report-01~05.jpg` 實作。報告含**九大項目**（國際營養基準/水分/飲食限制/成分學理/食安通報/藥食關聯/熱量試算/換食過渡/日誌時序）：**標題固定（寫死）、每項內容正式環境由 AI 生成**；圖中各項內容可當 mock — 驗證 ⏳
- [ ] 正式環境：主畫面與報告內容皆由 AI 分析產生（mock 僅供驗證長相；報告九大標題固定不變） — 驗證 ⏳

---

## Phase 5 — 主動推播：食安警報（新功能，P2）

**目標：** 系統主動推播「快訊中的食安警報」到使用者裝置，即使沒開 App 也能即時收到（如某產品/成分被列為食安風險）。
**相依：** Phase 4-1 快訊生成機制（需先有「食安警報」分類的快訊來源）。
**前置決策（需先拍板，見下方問題）：** 推播技術選 Web Push（VAPID + Service Worker）或 Firebase Cloud Messaging（FCM）。

### 5-A 技術選型與前置
- [ ] 決策：Web Push（VAPID，Vercel 原生、無第三方）vs FCM（frank 熟悉 Firebase，但要引入 Firebase） — 驗證 ⏳
- [ ] （若 Web Push）產生 VAPID 金鑰對，`VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` 放環境變數 — 驗證 ⏳
- [ ] iOS 限制確認：iOS 16.4+ 且須「加到主畫面」的 PWA 才能收 Web Push（需在 UX 說明） — 驗證 ⏳

### 5-B 訂閱基礎建設
- [ ] 新增 Service Worker（`public/sw.js` 或 next-pwa），處理 `push` / `notificationclick` 事件 — 驗證 ⏳
- [ ] 前端：請求通知權限的 UX（適當時機提示，非一進站就要權限） — 驗證 ⏳
- [ ] 前端：取得 PushSubscription 並送後端儲存 — 驗證 ⏳
- [ ] 新增 `PushSubscription` model（per user 的 endpoint/keys），及存/刪 API — 驗證 ⏳

### 5-C 食安警報觸發推播
- [ ] NewsArticle 需有「食安警報」分類/旗標（與 Phase 4-1 快訊生成對齊） — 驗證 ⏳
- [ ] 後端：當新的食安警報快訊產生時，對訂閱者發送 Web Push（標題、摘要、點擊導向該快訊） — 驗證 ⏳
- [ ] 防重複推播（同一則警報只推一次）；失效訂閱（410/404）自動清除 — 驗證 ⏳
- [ ] 推播觸發點：排程（Vercel Cron）或快訊生成流程內觸發 — 驗證 ⏳

### 5-D 設定與驗收
- [ ] 設定頁可開/關「食安警報推播」 — 驗證 ⏳
- [ ] 驗收：產生一則食安警報 → 已訂閱裝置收到推播 → 點擊導向快訊；關閉後不再收到 — 驗證 ⏳

---

## 進度總覽

| Phase | 主題 | 優先 | 估時 | 狀態 | 驗證 |
|-------|------|------|------|------|------|
| 0 | 快速清理與補資料 | P1/P3 | 半天 | ✅ 完成（0-1 保留；正式庫 migration 留 deploy 執行）| 部分✅/➖ |
| 1 | 共同飼主 Email 邀請（含權限修復、輪詢同步） | P1 | 2～3 天 | 🔄 1-A~1-E 完成；1-F 待 frank 實機端對端 | 1-B~1-E ✅；1-A ➖；1-F ⏳ |
| 2 | 首頁資料串接 + 日誌/飲食修正 | P1 | ~1 天 | 🔄 2-3/2-4 完成驗證；2-5 部分（前端硬化待做）；2-1/2-2 未開始 | 2-3/2-4 ✅；2-5 部分✅；2-1/2-2 ⏳ |
| 3 | 既有 UI 功能補完 + 導覽/版面調整 | P2 | 2～3 天 | ⬜ | ⏳ |
| 4 | AI 加值與空殼內容化 | P2 | 1～1.5 天 | ⬜ | ⏳ |
| 5 | 主動推播：食安警報 | P2 | 2～3 天 | ⬜ | ⏳ |

---

## 附錄：本次盤點補充驗證的事實
- `requirePetAccess` 已存在於 `src/lib/petAccess.ts`，但目前僅 `invitations`、`members` 兩支 API 使用；其餘約 25 支寵物資料 API 皆未做權限檢查（Phase 1-C 全列）。
- seed.ts 已建立 9 個 model，缺 8 個（Phase 0-3 全列）。

*P2 後續（撤銷/刪除邀請、邀請歷史分頁、通知發起者）見 `docs/未來功能.md`。*
*完成項目請更新勾選與進度總覽。*
