# Dr. Pet 待辦清單 TODO（分階段・細項版）

**來源：** `docs/系統盤點-2026-06-03.md` + 直接讀原始碼補充驗證
**建立日期：** 2026-06-03
**用途：** 把盤點文件中所有「尚缺／還沒做但該做」的項目，拆到檔案／API／步驟顆粒度，分階段追蹤，確保無遺漏。

> **追蹤改用兩個獨立維度：完成 + 驗證。**
>
> **完成（前方勾選框）：** `[ ]` 未開始 ｜ `[~]` 進行中 ｜ `[V]` 完成
> → 規則：**程式碼做完就打 `[V]`，不必等驗證通過**（驗證另以句尾標記表示）。
>
> **驗證（句尾標記）：** `✅` 驗證通過 ｜ `⏳` 待驗證 ｜ `➖` 不需驗證（frank 提供的參數/輸入/決策項）
>
> 每個細項格式範例：`- [V] 說明文字 — 驗證 ⏳`
>
> 優先度：P1 必修/影響使用 ｜ P2 功能完整性 ｜ P3 優化/加值

> ⚠️ **施工總則（工程師務必遵守）：**
> 1. 凡標有「**設計圖：`docs/features/ui-mockups/xxx.jpg`**」的項目，**實作前務必用 Read 工具開啟該圖、以圖為準、按圖施工**；待辦的文字僅輔助理解重點，**不取代看圖**。
> 2. 標示「mock / 正式環境由 AI…」的資料：現階段可用 mock 驗證長相，**正式環境須改由 AI 產生**，兩者都要做到。
> 3. 看不懂或圖文有出入時，先問 frank，不要自行臆測施工。

---

## 分階段規劃概覽

| Phase | 主題 | 優先 | 估時 | 排序理由 / 相依 | 狀態 | 驗證 |
|-------|------|------|------|----------------|------|------|
| **0** | 快速清理與補資料 | P1/P3 | 半天 | 零風險、最快見效；刪死碼 + 補 seed 讓多個空殼頁立刻有內容，後續開發也有資料可測 | ✅ 完成（正式庫 migration 留 deploy）| 部分✅/➖ |
| **1** | 共同飼主 Email 邀請（含權限修復、輪詢同步） | P1 | 2～3 天 | 已確認的當期主任務；牽動 25 支 API 權限補強 | 🔄 1-A~1-E 完成；1-F 待 frank 實機端對端 | 1-B~1-E ✅（review+QA邏輯層）；1-A ➖；1-F ⏳ |
| **2** | 首頁資料串接 + 日誌/飲食修正 | P1 | ~1 天 | 首頁串接 + frank 回報的日誌總覽/週曆/飲食 bug | ✅ 2-1~2-8 完成（2-6 真機待驗） | 2-1~2-5/2-7/2-8 ✅（review+QA/設計圖）；2-6 真機待 frank |
| **3** | 既有 UI 功能補完 + 導覽/版面調整 | P2 | 2～3 天 | 假按鈕變真功能；隱藏營養/諮詢頁、nav 重排、AI 諮詢移日誌浮動入口、毛孩頁快速入口 | ✅ 3-1/3-3~3-10 完成（3-2 併 Phase 5）| 多數 ✅（review+QA+設計圖）；3-3 語音/拍照/AI 對話 待實機；3-6 SOS 清單待 frank |
| **4** | AI 加值與空殼內容化 | P2 | 1～1.5 天 | 4-1~4-6 全做（快訊/症狀/月摘要/換食/添加項目/智能分析 AI）| ✅ 4-1~4-6 完成（review+build+設計圖）| AI 輸出品質/相機/實際長相 待 frank 有金鑰環境實機驗 |
| **5** | 主動推播：食安警報 | P2 | 2～3 天 | 新功能；**相依 Phase 4-1** 快訊生成（需有食安警報來源） | ⬜ | ⏳ |

**排序原則：** 先清零風險快速見效 → 當期主任務 → 依主題分批補完。
**總估時：** 約 6～8 個工作天（不含 frank 的 Gmail 憑證前置）。

---

## Phase 0 — 快速清理與補資料（零風險、立刻見效）

### 0-1 ~~刪除死碼 `chat` 頁~~ 🚫 決議：**先保留，不刪**（frank 2026-06-04 拍板）
- 維持現狀（已加棄用註解），本次 Phase 0 不刪除。後續要刪再啟動。

### 0-2 刪除死碼 `SwitchPlanComingSoon()`（P3）✅ 完成
- [V] 在 `src/app/diet/page.tsx` 找到 `SwitchPlanComingSoon()` 定義與呼叫點（grep 確認零呼叫） — 驗證 ✅
- [V] 確認換食計畫已改用 `DietSwitchPlan` 元件、此函式確實未被使用 — 驗證 ✅
- [V] 刪除函式與殘留 import（無殘留 import） — 驗證 ✅

### 0-3 補 Demo Mock Data（P2）✅ 完成（全掛 demo 寵物「布丁」）
- [V] `SymptomEntry` ×5 → 修復 `/symptoms` 空白 — 驗證 ✅
- [V] `ProductReaction` ×6（遵守 `@@unique([petId,productId,date])`）→ 修復日誌頁產品評分 — 驗證 ✅
- [V] `ChatMessage` ×4 → 修復 `/nutritionist` 無歷史 — 驗證 ✅
- [V] `InstantAnalysis` ×3（safe/caution/danger）→ 修復 `/scan` 歷史空白 — 驗證 ✅
- [V] `NutritionAnalysis` ×1 → 修復 `/nutrition` — 驗證 ✅
- [V] `NewsArticle` ×6（三分類各 2）→ 修復 `/news` 空殼 — 驗證 ✅
- [V] `AIInsight` ×1 → 修復 `/nutrition` 關聯分析 — 驗證 ✅
- [V] `WeeklyTask` ×4 → 修復 `/nutritionist` 計畫記錄 — 驗證 ✅
- [V] 執行 seed 成功（**僅對本機 dev.db**）、`tsc --noEmit` 通過。逐頁目視確認屬 QA 範疇（待 QA） — 驗證 ✅

> ⚠️ **附帶發現（超出原範圍，全端已處理/標記）：**
> 1. `schema.prisma` 原本缺 3 個 model（`NewsArticle`/`DailyMealPlan`/`MealPlanItem`）——存在於 migration 與 DB、程式碼也引用，卻沒寫進 schema，導致 generate 出的 client 沒有 `newsArticle`（NewsArticle seed 與 `/api/news` 執行期本來就會壞）。已依 migration 補回，validate 通過。**需架構/技術文件覆核。**
> 2. 🔴 `.env` 的 `DATABASE_URL` 指向**正式站 Turso**；直接 `npx prisma db seed` 會把假資料寫進正式庫。全端**未對正式庫寫入**，改用本機 dev.db。**需 DevOps 處理 seed 防呆 + 正式庫 migration 落後問題。**（見下方新增的 0-4）

### 0-4 Seed 防呆 + 資料策略 ✅（正式庫 migration 留 deploy 時做）
- [V] seed 防呆：`seed.ts` 檢查 DATABASE_URL，非 `file:` 即中止（已實測擋下正式 URL） — 驗證 ✅
- [V] demo/google 策略驗證：mock 全掛 demo-user/demo 寵物；全站無硬寫死 demo 寵物 id；`/api/pets` 依 userId 過濾；Google 新用戶首登乾淨、看不到 demo 資料（現況已正確，無需改碼） — 驗證 ✅
- [ ] 🔴 正式站 Turso migration 落後（缺 HealthMetric 等表）→ **deploy 時由總指揮+DevOps 安全執行**（寫正式庫的對外動作，不在本次 commit） — 驗證 ➖
- [ ] 🔴 **正式站關閉 demo 登入口**：Vercel 環境變數**不可**設 `DEMO_ENABLED` / `NEXT_PUBLIC_DEMO_ENABLED`（或設 false）→ 後端不註冊 demo provider、前端不顯示 demo 入口，demo 只存在測試環境、完全不碰正式 Turso。程式碼 gate 已就緒（`auth.ts` + `ClientShell.tsx`），純部署設定 — 驗證 ➖
- [V] 本機 dev.db 已由全端補跑 `add_diary_health_logs` migration（僅本機） — 驗證 ✅

### 0-5 QA 發現的顯示斷鏈（既有 bug，seed 補了也看不到）
> 以下是 QA 靜態分析發現的既有問題：資料有進 DB，但頁面查詢邏輯撈不到 / 無消費端。屬既有架構 bug，非 seed 之錯，登記避免遺漏。
- [V] SymptomEntry 第 4 筆 `eye` → 改 UI 支援類型（`tear`） — 驗證 ✅
- [V] **ProductReaction（產品評分/吃後感想）**（frank 最終決策）：屬 v1 功能、現版**不需要**。已從 `/diary` 移除 → 改放 **飲食頁 `/diet`** 但以旗標 `SHOW_PRODUCT_REACTIONS=false` **隱藏**；元件 `DailyReactionCard` 保留不刪、保持擴充彈性 → 列入 `docs/未來功能.md`，未來啟用把旗標改 true 即可（順帶保留的 `WeekCalendar` onDateSelect 週曆當日紀錄改善仍生效） — 驗證 ✅
- [V] **AIInsight 補顯示端**（frank 決策）：`/api/analyze` 加 GET，新增 `CorrelationInsights` 掛到 `/nutrition`，含空狀態 — 驗證 ✅
- [V] **`/symptoms` 抓錯寵物**：改為 client component、用 localStorage `drpet_currentPetId`，對齊全站 — 驗證 ✅
- [V] **demo 產品 `nutritional_facts`**：seed 已補範例成分 → `/nutrition` 營養圖表有資料 — 驗證 ✅
- [V] **WeeklyTask 無消費端**（frank 決策）：**保留 seed 不刪**（保留彈性）；「歷史觀察計畫」顯示功能尚不存在 → 已列入 `docs/未來功能.md`，未來需要再做 — 驗證 ✅

### 0-6 修正手機版頭像選單 + 補「切換毛孩」入口（frank 回報的 bug）✅ 完成
- [V] `AppShell.tsx` 手機版左上頭像原為純 div（無功能）→ 改為點擊開啟 **ProfileMenu**，與桌面 Sidebar 行為一致 — 驗證 ✅
- [V] 選單含「切換毛孩 / 設定 / 登出」三區；**補上手機版原本完全缺失的「切換毛孩」入口**（用 localStorage `drpet_currentPetId` + storage event 與其他頁同步） — 驗證 ✅
- [V] 修正先前誤把頭像直接導向 `/settings` 的錯誤（應為開選單，按「設定」才進設定頁） — 驗證 ✅
- [V] 同一 header 的鈴鐺按鈕原無功能 → 接到「通知/快訊」頁 `/news`（frank 拍板） — 驗證 ✅

---

## Phase 1 — 共同飼主 Email 邀請流程（當期主任務，P1）

### 1-A 前置：Gmail SMTP 寄信帳號 ⚠️【frank 要做的事，阻擋 1-B】
> 採 **Gmail SMTP + 應用程式密碼（App Password）**，不需 Google Cloud / OAuth / refresh token。
> 此方案本機與 Vercel 正式站皆可用；短期不申請網域也無需改 Resend。
> 寄件人顯示名由程式設為「無敏毛孩 PurePaw」，地址為下方 Gmail。
> （備註：原 Gmail API / OAuth 方案已棄用，太繁瑣且最終地址仍為 gmail。）

**步驟（只在 Gmail 帳號設定內操作，約 5 分鐘）**
- [V] 新辦一個專用 Gmail（建議，如 `purepaw.notify@gmail.com`，與私人信箱分開） — 驗證 ➖
- [V] 該帳號開啟「兩步驟驗證」（App Password 的前提） — 驗證 ➖
- [V] 產生一組「應用程式密碼」（16 碼） — 驗證 ➖

**📦 frank 要提供給 Claude 的 2 樣東西（放進環境變數）**
- [V] `GMAIL_USER` — 寄件 Gmail 信箱 — 驗證 ➖
- [V] `GMAIL_APP_PASSWORD` — 上一步的 16 碼應用程式密碼 — 驗證 ➖

**環境變數設定位置**
- [V] 本機 `.env`（供 `npm run dev` 測試） — 驗證 ➖
- [ ] 上 Vercel 時 → Settings → Environment Variables 同步填這 2 個值 — 驗證 ➖
- [ ] 確認 `NEXTAUTH_URL` 正式站指向 Vercel 網域（邀請信連結會用到，不能是 localhost） — 驗證 ➖

> 🔐 安全提醒：App Password 等同寄信權限，勿提交進 git、勿貼公開頻道，只放環境變數。
> 📌 升級路線：未來若買網域、想要 noreply@自有網域，再換 Resend；屆時只改 `sendEmail()` 一個函式。

### 1-B 寄信能力（後端）✅ 完成
- [V] 新增 `src/lib/email.ts`，封裝 `sendEmail({ to, subject, html })`，內部用 nodemailer + Gmail SMTP，寄件顯示名「無敏毛孩 PurePaw」 — 驗證 ✅
- [V] 設計邀請信 HTML 模板（毛孩名、邀請人、`/invite/{token}` 連結、7 天效期說明、品牌色 #C4714A） — 驗證 ✅
- [V] `src/app/api/pets/[id]/invitations/route.ts` POST：建立邀請後呼叫 `sendInviteEmail()` — 驗證 ✅
- [V] 寄信失敗的錯誤處理（try/catch + log + 回傳 `emailSent` 旗標，不阻斷邀請流程） — 驗證 ✅
- [V] `inviteUrl` 用 `NEXTAUTH_URL`（fallback `AUTH_URL`），非 localhost — 驗證 ✅
- [V] SMTP 實測：寄測試信成功（messageId 已回，需收件匣目視確認） — 驗證 ✅

### 1-C 共同飼主存取修復（必修 bug）
- [V] `src/app/api/pets/route.ts` GET：查詢從「只撈 `userId===自己`」改為 `OR: [{ userId }, { members: { some: { userId } } }]` — 驗證 ✅
- [V] 確認接受邀請時 owner 端也有 `PetMember(role: owner)` 紀錄（否則 OR 查詢對 owner 失效）；缺則於建立寵物時補建 owner membership，並寫一次性 migration 補既有資料 — 驗證 ✅
- [V] 補各「寵物資料 API」的 `requirePetAccess` 權限檢查（目前僅 invitations、members 有）：
  - [V] `api/daily-health-log` — 驗證 ✅
  - [V] `api/diary-records` — 驗證 ✅
  - [V] `api/diary-parse` — 驗證 ✅
  - [V] `api/health-metrics` — 驗證 ✅
  - [V] `api/measurement-record` — 驗證 ✅
  - [V] `api/medication-record` — 驗證 ✅
  - [V] `api/grooming-record` — 驗證 ✅
  - [V] `api/symptoms`、`api/symptoms/[id]` — 驗證 ✅
  - [V] `api/pet-products`、`api/pet-products/[id]` — 驗證 ✅
  - [V] `api/reactions` — 驗證 ✅
  - [V] `api/usages`、`api/usages/[id]` — 驗證 ✅
  - [V] `api/documents` — 驗證 ✅
  - [V] `api/meal-plans`、`api/meal-plans/[id]/items` — 驗證 ✅
  - [V] `api/instant-analyze`、`api/instant-analyze/[id]` — 驗證 ✅
  - [V] `api/nutrition-ai` — 驗證 ✅
  - [V] `api/diet-analysis` — 驗證 ✅
  - [V] `api/analyze`、`api/analysis` — 驗證 ✅
  - [V] `api/recommend` — 驗證 ✅
  - [V] `api/chat` — 驗證 ✅
  - [V] `api/tasks` — 驗證 ✅
  - [V] `api/community/recs`、`api/community/trigger` — 驗證 ✅
  - [V] `api/pets/[id]`（單一寵物讀寫，含刪除權限限 owner） — 驗證 ✅
  - [V] 逐支檢視：讀取類允許 owner+co_owner；刪除毛孩等破壞性操作僅限 owner — 驗證 ✅

### 1-D 共享資料即時同步（輪詢 + 聚焦重抓）
- [V] 決定實作方式（SWR/React Query 的 `refreshInterval` + `revalidateOnFocus`，或自寫 `setInterval` + `visibilitychange`） — 驗證 ✅
- [V] 抽共用 hook（如 `usePolledFetch`），間隔 20–30 秒 — 驗證 ✅
- [V] 套用到共享資料頁面：
  - [V] 首頁 `src/app/page.tsx` — 驗證 ✅
  - [V] 日誌 `src/app/diary/page.tsx` — 驗證 ✅
  - [V] 飲食 `src/app/diet/page.tsx` — 驗證 ✅
  - [V] 寵物詳情 `src/app/pet/[id]/page.tsx` — 驗證 ✅
  - [V] 其餘顯示寵物資料的頁面視需要加上 — 驗證 ✅
- [V] 確認背景分頁不浪費請求（聚焦才高頻、失焦降頻或停） — 驗證 ✅

### 1-E QRCode 保留當備用
- [V] 確認 `settings/page.tsx` 的 email 邀請為主要動線、QRCode 區塊調整為「或當面掃碼分享」次要位置 — 驗證 ✅
- [V] 文案調整：說明已寄信 + QRCode 為備用 — 驗證 ✅

### 1-F 驗收測試
- [ ] A 帳號邀請 B 的 email → B 信箱真的收到信 — 驗證 ⏳
- [ ] B 點信中連結 → 進登入頁 → Google 登入 → 跳法尊 → 同意 → 成為 co_owner — 驗證 ⏳
- [ ] B 首頁看得到被分享的毛孩（阿東） — 驗證 ⏳
- [ ] B 可新增/編輯該毛孩紀錄；A 端輪詢內自動看到更新 — 驗證 ⏳
- [ ] B 不能存取「沒被分享的別人毛孩」（權限阻擋有效） — 驗證 ⏳
- [ ] 錯帳號登入、過期邀請、重複接受等邊界情境正確擋下 — 驗證 ⏳

> 不在範圍（見 `docs/未來功能.md`）：撤銷/刪除邀請、邀請歷史分頁、通知發起者

---

## Phase 2 — 首頁資料串接 + 日誌/飲食修正（P1）

> 註：本 Phase 原為「首頁資料串接」(2-1/2-2)，後併入 frank 回報的日誌/飲食 bug（2-3 月曆總覽、2-4 週曆互動、2-5 飲食頁）。2-3/2-4/2-5 已實作，**尚未 commit、待 frank 實機驗證**後收尾。

### 2-1 首頁「年度疫苗 / 體外驅蟲」
- [V] `src/app/page.tsx` L383/388 移除 hardcoded `--` — 驗證 ✅
- [V] fetch MedicationRecord（疫苗/驅蟲類）；無對應 API 則確認/擴充 `api/medication-record` 查詢 — 驗證 ✅
- [V] 顯示最近一次 + 下一次到期推估（依紀錄頻率） — 驗證 ✅
- [V] 無資料時顯示「尚未記錄」而非 `--` — 驗證 ✅

### 2-2 首頁「未來日程表」醫療/美容 tab
- [V] `src/app/page.tsx` L585 串 MedicationRecord / GroomingRecord 的未來排程 — 驗證 ✅
- [V] 醫療 tab、美容 tab 各自顯示對應資料 — 驗證 ✅
- [V] 真的無資料時才顯示「尚無日程記錄」 — 驗證 ✅

### 2-3 日誌月曆總覽修正（frank 回報，P1）
> 診斷結論：主要是串資料/邏輯 bug，非 mock 問題。核心修法：總覽**統一以健康日誌 DailyHealthLog 為基準**，並為各觀察欄位定義「正常值白名單」。決策：先修 3 個 bug + 加白話摘要，版面重新設計留待之後（見未來功能）。
- [V] **bug1 情緒誤判**：`MonthHealthOverview.tsx:166-171` 只要 mood 有值就算異常 → 改為「非正常值才算異常」（為 mood 等多選觀察欄位定義正常值白名單，如「平靜放鬆」） — 驗證 ✅
- [V] **bug2 健康良好 -4**：`L243` `recordedCount − abnormalDays` 兩者來自不同表（飲食 vs 健康日誌）→ 改為全部用 DailyHealthLog 計算（良好 = 有日誌天數 − 異常天數） — 驗證 ✅
- [V] **bug3 已紀錄 0/30**：`L138` 改用「健康日誌有紀錄的天數」為基準（frank 決策），不再用 ProductUsage — 驗證 ✅
- [V] **加一行白話摘要**：總覽最上方加一句總結（例：「本月 18 天有紀錄，14 天狀況良好，4 天有軟便」），並修正各統計標籤語意 — 驗證 ✅
- [V] **mock 小修**：seed 的 mood「活潑好動」不在 MoodCard 選項中 → 統一（補進選項或改 seed 值） — 驗證 ✅

### 2-5 飲食頁「繼續添加項目」卡在「建立配餐計畫中…」（frank 回報，P1）
> 根因：`model DailyMealPlan` 在 v1.6.0（commit f595dad 日誌改版）被誤從 schema.prisma 移除 → prisma client 沒有 `dailyMealPlan` → `/api/meal-plans` 建立計畫 500 → 前端 `ensurePlan()` 回 null → 無限轉圈。**後端已於 Phase 0（commit 3292e4e）補回 model，本機 upsert 實測 OK。**
- [V] schema 補回 `DailyMealPlan`（已隨 3292e4e 完成） — 驗證 ✅
- [V] 確認部署/開發環境 `npx prisma generate` + 重啟，飲食頁恢復新增項目（已實測 API 201） — 驗證 ✅
- [ ] 正式站 Turso 需確認有此表（併入 0-4 正式庫 migration 一起處理） — 驗證 ➖
- [V] **前端硬化**：`ensurePlan()` 失敗時不要無限轉圈 → 顯示錯誤訊息 + 可重試（`diet/page.tsx` ensurePlan / SessionAccordionWithPlan） — 驗證 ✅
- [V] **修觸發漏洞**（frank 回報又一直轉）：建立計畫原只在「手動展開時段」觸發，預設展開的早餐沒經 toggle → planId 一直 null → 卡轉圈。改為「繼續添加項目」按下時若無 planId 就主動建立 — 驗證 ⏳（待 frank 重整實測）

### 2-4 日誌月曆/週曆互動修正（frank 回報，P1，與 2-3 同批）
> 診斷結論：日曆「點點」與「已紀錄天數」資料來源不統一；週曆寫死本週、點日期不連動、且有一塊重複的空區塊。
- [V] **點點資料源統一**：日期下方「有紀錄圓點」目前只看 ProductUsage（`page.tsx:1250-1257`），demo 無飲食紀錄就永遠不顯示 → 改為「該天有任何紀錄就顯示點」（涵蓋 DailyHealthLog + ProductUsage + 症狀/用藥/美容/量測等），月曆與週曆一致 — 驗證 ✅
- [V] **已紀錄天數語意對齊**：上方「已紀錄 X 天」要與點點同義（有紀錄的不重複天數，例：6/1~6/4 各有紀錄=4 天）；與 2-3 的基準統一 — 驗證 ✅
- [V] **週曆點日期連動**：點週曆某天，下方當日紀錄要跟著換（`WeekCalendar` 改受控、與頁面 `selectedDate` 同步，確認 `HealthLogSection` 吃到正確 date；排查時區/內部 state 衝突） — 驗證 ✅
- [V] **隱藏/修正空的「(日期)的紀錄」區塊**：週曆點日期會冒出一塊看似要顯示總結卻空白的區塊（疑似 `WeekCalendar` 內建 `DayDetail`）→ 與下方連動的紀錄重複；決策：移除/隱藏這塊（frank：不顯示總結就先隱藏） — 驗證 ✅
- [V] **週曆可切換週**：月曆能換月，週曆要能上一週/下一週（`page.tsx:411-447` 寫死本週 → 加 anchor state + 上/下週導覽，可參考 `DiaryCalendar.tsx` 既有 WeekView） — 驗證 ✅
- [V] **mock 每日資料變化**（frank 回報「每天長一樣」）：seed 原本最近 6 天都對應「正常」樣態 → 改為 6 種樣態輪替，6/1~6/4 各自不同（已查 DB 確認）；並修正既有不合法值（`軟便`→`便便偏軟`、`帶黏液` 移到 stoolDetails）。已重 seed dev.db — 驗證 ✅（DB 已確認；畫面待 frank 重整點擊）

### 2-6 手機版 bottom-sheet modal 無法捲動、完成按鈕被擋（frank 回報，P1 bug）
- [V] 週曆「用藥看診 / 洗澡美容 / 量測紀錄」點開的 bottom-sheet modal，在**手機版無法捲動** → 最下面「完成紀錄」按鈕被擋住，無法送出 — 驗證 ⏳
- [V] 修 modal 的 scroll（高度/overflow/safe-area），確保手機版可捲到底並按到完成；桌面版不受影響 — 驗證 ⏳

### 2-7 飲食頁早/午/晚餐有項目時的展開/收合顯示（frank 回報）
> 設計圖：展開 `docs/features/ui-mockups/diet-meal-session-expanded.jpg`；收合 `docs/features/ui-mockups/diet-meal-session-collapsed.jpg`
- [V] 有項目時，**展開**狀態（見 expanded 圖）：逐項顯示產品名 + 標籤 + 數量/單位 + 預估克數 + 「繼續添加項目」 — 驗證 ✅
- [V] **收合**狀態（見 collapsed 圖）：精簡列出「產品名 (數量 單位)」；未添加則顯示「尚未添加配餐項目」 — 驗證 ✅

### 2-8 飲食頁「換食計畫」畫面對照設計圖（frank 提出）
> 現況：換食計畫（`DietSwitchPlan` 元件）已實作；需對照設計圖確認長相、缺資料則補 mock。
> 設計圖：`docs/features/ui-mockups/diet-switch-plan-01.jpg`（換食計畫入口點下去後的畫面）
- [V] 對照設計圖確認畫面一致；**不一致則改成與設計圖一致** — 驗證 ✅
- [V] 畫面區塊：當前測試商品（測試中第 N/14 天、配方名、替換目標、標籤）／7 天換食排程建議（今日建議比例 新:舊 + 進度條）／身體特徵監控（大便分數、抓癢頻率、趨勢）／底部「晉升日常飲食」「淘汰並更換」兩鈕 — 驗證 ✅
- [V] 缺 mock_data 則補（測試商品、排程比例、身體特徵監控數據）以供驗證；正式環境接真實資料 — 驗證 ✅

---

## Phase 3 — 既有 UI 功能補完（P2）

### 3-1 頭像上傳
- [V] `src/app/settings/page.tsx` L392 移除 `disabled` 與「即將推出」title — 驗證 ✅
- [V] 串 `api/upload`，上傳後寫回 Pet/User 的 avatar 欄位 — 驗證 ✅
- [V] 上傳中 loading、失敗提示、預覽 — 驗證 ✅

### 3-2 提醒機制（用藥/美容）— frank 決策：**Web Push 推播**
> ⚠️ frank 拍板用「瀏覽器推播 Web Push」。這需要推播基礎建設（Service Worker + VAPID + 訂閱），**與 Phase 5 食安推播共用同一套**。
> → **本項與 Phase 5-A/5-B 推播基礎一起做**（避免造兩次輪子）；Phase 3 先不單獨實作，待 push 基礎建好後再接上「設定提醒」。
> 註：DailyChecklist 的「設定」已改由 3-7「每日任務管理 modal」定義，不在本項。
- [ ] （待 Phase 5 推播基礎）用藥 Modal「設定提醒」取代 `alert`（`MedicationModal.tsx` L321）→ 建立 Web Push 提醒 — 驗證 ⏳
- [ ] （待 Phase 5 推播基礎）美容 Modal「設定提醒」取代 `alert`（`GroomingModal.tsx` L287）→ 建立 Web Push 提醒 — 驗證 ⏳

### 3-3 SmartMemo 語音輸入
- [V] `src/app/diary/page.tsx` L516 移除 `disabled` — 驗證 ✅
- [V] 串 Web Speech API（語音轉文字），含瀏覽器不支援的降級處理 — 驗證 ⏳
- [V] 錄音中狀態 UI、權限拒絕提示 — 驗證 ✅

### 3-4 隱藏「營養分析」「AI 諮詢」頁 + nav 重排（frank 提出）
> 共通備註：以下「modal」均指像日誌週曆「用藥看診」那種**從下方滑出的 bottom sheet**。
- [V] 隱藏「營養分析」頁（電腦左側 nav / 手機下方「營養」）→ 先隱藏、日後可開（保留程式碼，nav 移除入口） — 驗證 ✅
- [V] 隱藏「AI 諮詢」頁（`/nutritionist`）→ 先隱藏、日後可開（保留程式碼，nav 移除入口；功能改掛日誌頁見 3-5） — 驗證 ✅
- [V] nav 重排為 **毛孩｜飲食｜照相｜日誌｜快訊**（`Sidebar.tsx` 電腦版 + `BottomNav.tsx` 手機版同步） — 驗證 ✅

### 3-5 AI 諮詢移為日誌頁浮動入口 + modal（frank 提出，相依 3-4）
- [V] 在日誌頁加一個**置底+置右、隨頁面浮動的圓形 icon** 入口（fixed 定位、滑動時固定） — 驗證 ✅
- [V] 點 icon → 開 bottom-sheet modal，內容為 AI 諮詢功能（複製/重用 `/nutritionist` 的對話功能） — 驗證 ✅

### 3-6 毛孩頁「快速入口」加 3 個（frank 提出）
> 設計圖：`docs/features/ui-mockups/home-quick-entries.jpg`（按圖施工）
- [V] 新增三個入口：**日查觀察表**（DAILY OBSERVATION FORM）、**就醫記錄表**（MEDICAL VISIT RECORD）、**緊急協助 SOS**（EMERGENCY ASSISTANCE，紅底強調） — 驗證 ✅
- [V] 去向（frank 拍板）：日查觀察表 → 日誌健康填寫；就醫記錄表 → 用藥看診紀錄 — 驗證 ✅
- [V] **緊急協助 SOS** → 開急救醫院清單（44 間，來源 `docs/emergencyList/醫院資料整理.md`）。已升級為**四組分類篩選**（寵物種類→地區→症狀科別→特殊資源；組內 OR/組間 AND）**＋文字模糊搜尋**（名稱/地區/地址）＋營業時間顯示＋**電話點擊撥號含確認**＋medium/待確認核實提示＋空狀態。清單資料已由 frank 提供並填入 — 驗證 ⏳（實機）

### 3-7 每日任務管理 modal（「每日紀錄項目」的「設定」連結，frank 提出）
> 即系統盤點裡 `DailyChecklist.tsx` L70「設定功能即將推出」alert 的完整定義；取代 3-2 的 Checklist 設定 alert。
> 註：UI 是「每日紀錄項目」區塊**右上角的「設定」文字連結**（非齒輪 icon）。
> 設計圖：入口 `docs/features/ui-mockups/diary-daily-record-block.jpg`；modal `diary-daily-task-modal-1.jpg`、`diary-daily-task-modal-2.jpg`
- [V] 日誌週曆「每日紀錄項目」區塊右上角的**「設定」文字連結**點下去 → 開 bottom-sheet modal「每日任務管理」 — 驗證 ✅
- [V] modal 內每個任務：開/關 toggle + 備註欄（如「刷牙清潔／使用紗布刷牙加牙粉」）；支援「新增自訂任務」（如點眼藥水）、「全部取消」、底部「完成設定」 — 驗證 ✅
- [V] 連動「每日記錄項目」區塊：新增任務**預設開啟**；**關閉的任務在該區塊隱藏**；該區塊只顯示目前開啟的任務（開幾個顯示幾個） — 驗證 ✅
- [V] 任務清單 + 開關 + 備註需持久化（評估用既有 `useRecordParams` / `dailyChecklist` 欄位，或新資料結構）；確認與既有 DailyChecklist 完成勾選相容 — 驗證 ✅

### 3-8 日誌月曆縮小 + 月/週切換移到右上角（frank 回報）
- [V] 月曆整體**縮小**：建議比例**約 0.85（cell 高度/字級/間距同步縮約 15%）**，frank 可再微調 — 驗證 ✅
- [V] 月/週切換**移到月曆右上角**（設計圖 `docs/features/ui-mockups/diary-calendar-toggle.jpg`）：月曆時顯示「週檢視」、週曆時顯示「月檢視」，點擊切換 — 驗證 ✅
- [V] **隱藏**日誌頁最上面那排月曆/週曆 tab；切換後既有功能（圓點、連動、切月/切週）全部不變 — 驗證 ✅

### 3-9 週曆健康區塊右上角裝飾 icon 改為不可點（frank 回報，bug）✅ 完成（拍照已保留）
- [V] 4 張卡（皮膚毛髮/牙齒口腔/消化/眼耳）右上角相機 icon 已改為不可點、不再觸發 — 驗證 ✅
- [V] ⚠️ **修正**：第一波把這 4 張卡的「拍照上傳」整個移除了。frank 決策：**拍照功能要保留**，只是讓那顆 icon 不可點/不觸發即可 → 需把上傳能力加回（改由別處觸發或保留入口），icon 維持純裝飾不可點 — 驗證 ✅

### 3-10 毛孩頁「健康檔案概覽」隱藏活力指數/水分攝取（frank 提出）
- [V] 毛孩頁「健康檔案概覽」中的**活力指數**與**水分攝取**先隱藏（保留程式碼，日後可開）；同步登記到 `docs/隱藏與保留項目.md` — 驗證 ✅

---

## Phase 4 — AI 加值與空殼頁面內容化（P2）

### 4-1 快訊 AI 生成機制
- [V] 設計 AI 定期生成快訊流程（`api/news/crawl` 已存在，確認其行為） — 驗證 ⏳
- [V] 排程觸發（Vercel Cron 或手動觸發）寫入 NewsArticle — 驗證 ⏳
- [V] `/news` 三分類顯示真實生成內容 — 驗證 ⏳

### 4-2 症狀詳情頁 AI 建議
- [V] `src/app/symptoms/[type]/page.tsx` 串 `api/chat`（或新 endpoint） — 驗證 ⏳
- [V] 依症狀紀錄產生「可能原因 / 建議做法」，附 `VET_REFERENCE_SCOPE` — 驗證 ⏳
- [V] 結果快取，避免每次重打 AI — 驗證 ⏳

### 4-3 日誌月摘要 AI 解讀
- [V] `MonthHealthOverview` 統計數字下方加一段 AI 解讀 — 驗證 ⏳
- [V] 串 AI endpoint，依當月統計生成；無資料不顯示 — 驗證 ⏳

### 4-4 飲食頁「添加項目」改 bottom-sheet modal + AI 產品搜尋選取（frank 提出，含原 item 5）
> 現況：飲食頁「新增項目」點下去是**下方 inline 展開**自由文字框，無法檢核對錯、易打錯 → 之後 AI 分析不準。
> 目標：(1) 改成點擊**開 bottom-sheet modal**（從下方滑出，樣式同日誌「用藥看診」）；(2) modal 內為 AI 輔助搜尋選取（版面如下方「modal 版面描述」）。
>
> **設計圖（以圖為準）：** `docs/features/ui-mockups/diet-add-item-ai-search.jpg`（modal 整體 + 卡片收合態）；
> 展開細節：`diet-add-item-detail-info-1.jpg`、`diet-add-item-detail-info-2.jpg`。
>
> **modal 版面描述（文字備援，細節以上述圖為準）：**
> - 頂部：**搜尋輸入框**（放大鏡圖示 + placeholder，右側可放語音 mic 圖示），打字即觸發 AI 搜尋。
> - 輸入框下：**分類篩選 chips**（全部商品 / 飼料 / 保健品 / 鮮食…）。
> - 標題列：「**AI 智能搜尋結果 (N)**」。
> - **結果卡片**（收合態，見 `diet-add-item-ai-search.jpg`）：產品分類標籤、「已加入」徽章、右上「＋」加入鈕、產品名稱、品牌+規格、可展開的「完整成分資訊（與包裝一致）」摘要、驗證標章（ISO 22000/AAFCO/FEDIAF）、AI 驗證數據出處 + 更新日期。
> - 底部：找不到時可「自訂新增」備援。
>
> **「完整成分資訊」展開／收合（frank 補充，見 detail-info 兩張圖）：**
> - **收合**：只顯示 `diet-add-item-ai-search.jpg` 那樣的基本成分摘要。
> - **展開**：顯示 detail-info-1/2 的完整細節——完整成分、營養添加物(每公斤)、營養成分表(蛋白/脂肪/鈣磷…+熱量)、商品規格(適用年齡/產地/功能/穀類/口味/軟硬度/顆粒)、製造與代理資訊、每日建議餵食量(體重→餵食量表)、錯誤回報。
> - **資料來源**：detail-info 圖中的資料**現階段可當 mock_data**；**正式環境必須由 AI 在搜尋產品時一併抓取該產品的詳細資料**填入展開內容（不是寫死）。
- [V] 「添加項目」改為開 bottom-sheet modal（取代現有 inline 展開的 AddItemForm） — 驗證 ⏳
- [V] modal 內依設計圖 `diet-add-item-ai-search.jpg` 實作：搜尋框 + 分類 chips + AI 結果卡片 + 加入鈕；保留「找不到時可自訂」備援 — 驗證 ⏳
- [V] 卡片「完整成分資訊」可展開：收合如 ai-search 圖、展開如 detail-info-1/2 圖（成分/營養添加物/營養成分表/商品規格/製造代理/每日建議餵食量/錯誤回報） — 驗證 ⏳
- [V] **搜尋結果產品有風險時的呈現**（設計圖 `diet-add-item-ai-search-risk-1.jpg`、`-risk-2.jpg`）：產品卡需依風險顯示警示，有兩種樣式：
  - **風險樣式 A — 產品風險（risk-1 圖）**：分類標籤旁加紅色警示 chip「⚠️ 建議謹慎使用」；卡片內多一塊紅色 **「⚠️ 風險說明」** 段落，文字描述具體風險（例：「該品牌旗下其他批次曾於 2024 抽驗發現重金屬超標，本款雖有檢驗報告但品牌合規記錄存在瑕疵，建議購買前再核對最新批次」）；數據出處指向風險來源（例：食藥署不合格紀錄、原廠回覆函）+ 數據更新日期。
  - **風險樣式 B — 品牌警示（risk-2 圖）**：產品名稱上方加一條紅色橫幅 **「⚠️ {品牌}（品牌警示：多項產品未通過核可）」**；產品本身仍可同時顯示正向驗證標章（如 IFOS 五星、SGS 重金屬檢驗）——即「品牌有疑慮但本品有檢驗」並陳。
  - **無風險**：照原 `diet-add-item-ai-search.jpg` 正常顯示（不出現警示 chip / 風險說明 / 品牌橫幅）。
  - **資料來源**：圖中風險文字現階段可當 mock；**正式環境由 AI 綜合「該毛孩健康狀態 + 主要問題 + 過敏原 + 飲食禁忌」與「該產品成分 / 品牌合規 / 食安通報紀錄」分析，判定此產品是否對這隻毛孩有害／需謹慎，再決定要不要顯示哪種風險樣式與文字**。 — 驗證 ⏳
- [V] 串接產品搜尋（可用既有 `/api/products/web-search` AI 找產品 / `/api/products/lookup`）；**正式環境 AI 需一併抓回產品詳細資料供展開顯示**（detail-info 內容），現階段可先用 mock；加 debounce、loading、空結果處理 — 驗證 ⏳
- [V] 選取後存標準化產品（productId 綁定 + 詳細資料），讓營養/成分分析能對應到真實產品資料 — 驗證 ⏳

### 4-5 飲食頁「AI 智能分析配餐」結果畫面對照設計圖（frank 提出）
> 現況：此功能（`/api/diet-analysis`、`DietAnalysisResult.detailedReport`）**已實作，但因缺 mock_data 從未驗證過長相**。
> 設計圖（已上傳）：
> - 主畫面連貫圖 `docs/features/ui-mockups/diet-ai-analysis-01.jpg ~ -06.jpg`（由上到下標示各資訊位置與內容）
> - 詳細報告 modal `diet-ai-analysis-report-01.jpg ~ -05.jpg`
> 圖中資訊現階段可當 mock；**正式環境須由 AI 分析產生**。
- [V] 補 mock_data（`DietAnalysisResult` / `detailedReport`）讓「AI 智能分析配餐」結果畫面能呈現以供驗證 — 驗證 ⏳
- [V] 逐區對照主畫面設計圖（01~06）：確認實際畫面與設計一致；**不一致則改成與設計圖一致** — 驗證 ⏳
- [V] **四個分類資訊標籤按鈕**（對應 03~06）：各標籤切換顯示對應內容 — 驗證 ⏳
- [V] **04 的「自拍成分」相機**：點相機 → 手機開相機 / 電腦版上傳圖片（接既有 instant-analyze 或對應流程） — 驗證 ⏳
- [V] **06 的「點入詳細分析報告」按鈕** → 開 bottom-sheet modal，內容依 `diet-ai-analysis-report-01~05.jpg` 實作。報告含**九大項目**（國際營養基準/水分/飲食限制/成分學理/食安通報/藥食關聯/熱量試算/換食過渡/日誌時序）：**標題固定（寫死）、每項內容正式環境由 AI 生成**；圖中各項內容可當 mock — 驗證 ⏳
- [V] 正式環境：主畫面與報告內容皆由 AI 分析產生（mock 僅供驗證長相；報告九大標題固定不變） — 驗證 ⏳

### 4-6 換食計畫串 AI（frank 提出）
> 現況：換食計畫畫面（`DietSwitchPlan`，待辦 2-8）已依設計圖做好，但**內容全是 mock 常數**（測試商品/排程比例/身體特徵監控皆寫死）。本項把它接上 AI。
> 設計圖：`docs/features/ui-mockups/diet-switch-plan-01.jpg`（畫面不變，改由 AI 供資料）。
- [V] **7 天換食排程建議（今日建議比例 新:舊 + 進度條）**：改由 AI 依「換食第幾天 + 毛孩近日身體反應（日誌排便/皮膚等）」動態建議比例，取代 mock 寫死的 1:3 — 驗證 ⏳
- [V] **身體特徵監控（大便分數 / 抓癢頻率 / 趨勢）**：改由 AI 從該毛孩日誌資料（`DailyHealthLog` 排便型態、皮膚搔抓等）分析計算與趨勢判讀，取代 mock — 驗證 ⏳
- [V] **晉升日常飲食 / 淘汰並更換 建議**：AI 依監控結果給「是否適應良好、可晉升或該淘汰」的判斷與提示 — 驗證 ⏳
- [V] 新增/沿用 AI endpoint（評估擴充 `/api/diet-analysis` 或新 `/api/switch-plan-ai`）；mock 僅供驗證長相，正式環境改吃 AI；無足夠資料時的降級顯示 — 驗證 ⏳
- [V] **「結束計畫」按鈕**（frank 提出）：在換食計畫畫面補一個「結束計畫」入口，讓使用者可**隨時臨時終止**換食計畫（有別於「晉升日常飲食／淘汰並更換」那種有結論的結束，這是單純中止）；按下後清除進行中計畫狀態、回到未啟動換食的畫面（需確認 + 可取消，避免誤按） — 驗證 ⏳

### 4-7 AI/外部端點帳號分流：demo 全 mock、真實+測試全 AI（frank 拍板，核心原則）
> 原則：**只要是 demo 帳號（`demo@drpet.com`/`demo-user`）就全走 mock**（不打 Anthropic/不打外部搜尋）；**測試 + 真實環境帳號全走真實 + AI**。判定在後端、看 session（`src/lib/demo.ts` 的 `isDemoUser`）。
- [V] 新增 `src/lib/demo.ts` `isDemoUser(session)`（id===demo-user 或 email===demo@drpet.com） — 驗證 ✅
- [V] 15 支 AI/外部 endpoint 加分流（demo→mock、其餘→AI）：analyze / chat / diary-parse / diet-analysis / extract / instant-analyze / month-summary / nutrition-ai / products/lookup / products/web-search / recommend / switch-plan-ai / symptoms/advice / tasks / news（讀取）— 驗證 ✅（金鑰實打驗證有效；runtime 分流待 frank 瀏覽器驗）
- [V] `news/crawl`（排程生成）維持真 AI，不分流（非帳號觸發）— 驗證 ✅
- [V] 一般資料（寵物/日誌/產品）：demo 吃 seed、真實吃自己資料（Phase 1-C 既有資料歸屬分流）— 驗證 ✅

---

## Phase 5 — 主動推播：食安警報 + 用藥/美容提醒（Web Push，P2）

**技術：Web Push（VAPID + Service Worker）**（frank 拍板）。設計規格見 `docs/features/push/push-spec.md`。
**前置已備：** `npm install web-push`、VAPID 金鑰已產並寫入 `.env`（`VAPID_PUBLIC_KEY`/`NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`）。
**frank 決策彙整：** ①食安只推「緊急」+ 個人化頭條（見 5-C）②PWA manifest 全平台都做 ③食安/提醒兩個獨立開關 ④提醒一次性+週期性都做 ⑤**demo 走觸發、內容 mock**（frank 後改：所有操作跟一般帳號一樣走真實測試 DB，只差推播內容是 mock；非原 no-op）⑥提醒 cron 每日台灣 08:00。

### 5-A 前置（已完成）
- [V] 裝 web-push、產 VAPID 金鑰寫入 `.env`（上 Vercel 要同步這 4 個變數）— 驗證 ✅
- [ ] iOS 限制：iOS 16.4+ 且「加到主畫面」PWA 才能收；UI 要提示/降級 — 驗證 ⏳

### 5-B 推播基礎建設 ✅
- [V] 新增 `PushSubscription` model（userId cascade、endpoint @unique、p256dh、auth、`foodAlertEnabled`、`reminderEnabled`）+ migration（本機跑、正式留 deploy）— 驗證 ✅（build）
- [V] `public/sw.js`：處理 `push` / `notificationclick`（點擊導向對應快訊/頁面）— 驗證 ✅（review）
- [V] `src/lib/push.ts`：web-push + VAPID 發送 helper；失效訂閱（410/404）自動刪除；分批並行送出（`sendInBatches` 每批 50、`Promise.allSettled`）— 驗證 ✅（review）
- [V] 訂閱/退訂 API（`/api/push/subscribe`、`/api/push/unsubscribe`）；**demo 走真實訂閱/退訂（與一般帳號一致），只差推播內容 mock** — 驗證 ✅（review）
- [V] **PWA manifest（全平台：iOS/Android/桌機）** + 品牌 icon（用現有 app-logo），讓「加到主畫面」可裝、iOS 能收推播 — 驗證 ✅（build）；iOS 實機收推播 待 frank ⏳

### 5-C 食安警報觸發推播（含個人化頭條）✅
- [V] NewsArticle 標記「食安警報」+「緊急」旗標（對齊 4-1 生成；加 `affectedBrands` + `foodAlertPushedAt` 防重複）— 驗證 ✅（review）
- [V] 在 `news/crawl` 產生**緊急**食安警報時，對「開食安推播」的訂閱者發送（標題/摘要/點擊導向該快訊）；同文章只推一次（`foodAlertPushedAt` 去重）— 驗證 ✅（review）
- [V] **個人化頭條**（frank 要求）：警報中的**廠商/產品** ∩ 該使用者毛孩**正在食用的產品/廠商**（比對 PetProduct）→ 該使用者收到的推播 **highlight + 頭條優先**（`headline`/`priority` 旗標）— 驗證 ✅（review）
- [V] demo 帳號走觸發、推播內容 mock（`push.ts` 送出當下換 mock，不寫進 DB）— 驗證 ✅

### 5-D 用藥/美容提醒（Web Push，併入原 3-2）✅
- [V] `MedicationModal`/`GroomingModal` 的「設定提醒」取代 alert：`ReminderSetter`（日期 + 一次性/週期性預設）— 驗證 ✅（review）
- [V] **一次性提醒**：到期當天推一次後清除 `nextReminder` — 驗證 ✅（review）
- [V] **週期性提醒**（frank 要求，如每月驅蟲/定期美容）：`reminderIntervalDays` 欄位，到期推播後自動順延下次 — 驗證 ✅（review）
- [V] cron `/api/reminders/check`（每日台灣 08:00=UTC 00:00、沿用 CRON_SECRET）：檢查當天到期提醒（台灣時區 due 判定）→ 對「開提醒推播」訂閱者發送 — 驗證 ✅（review）

### 5-E 設定與驗收
- [V] 設定頁：**食安警報推播**、**用藥/美容提醒**兩個獨立開關（請求權限 + 訂閱/退訂）— 驗證 ✅（review）
- [ ] 驗收（實機）：緊急食安警報 → 訂閱裝置收到（命中自己產品時為頭條）→ 點擊導向快訊；提醒到期收到；關閉後不再收到；demo 收 mock 推播 — 驗證 ⏳（待 frank 實機）

---

## Phase 6 — 飲食頁測試回饋修正（frank 實測後提出，P1）

**來源：** frank 2026-06-10 飲食頁實測回饋。**施工方式：** fullstack（DB/API→前端）接力 → code-reviewer 審 → tech-writer 補三文件+隱藏項目文件；全做完 tsc+build 綠燈後整包給 frank 自行 commit（排除 dev.db）。

### 6-0 新增項目 modal 置中 ✅（已完成待 commit）
- [V] `AddItemModal.tsx` 由底部 bottom-sheet（`justify-end`/`rounded-t-3xl`/safe-area pad）改為**正中央浮空**（`items-center justify-center p-4`/`rounded-3xl`/`pb-6`）；手機/桌機皆置中，過長內捲（`max-h-[90dvh]`）— 驗證 ⏳（待 frank 實機）

### 6-1 錯誤回報按鈕 → 真功能
> 現況 `AddItemModal.tsx` 錯誤回報為假按鈕（只 `alert('已收到…')`，不存不寄）。
- [V] 新增 `ProductErrorReport` model（`productId` 關聯 Product cascade、`userId`、`note String?`、`status @default("pending")`、`createdAt`）+ migration（**只跑 dev.db**：用 db execute 建表 + `migrate resolve --applied` 對齊，未 reset；正式庫留 deploy）— 驗證 ✅（build）
- [V] 新增 `POST /api/product-report`：需登入；帶 productId(+選填 note)；**所有帳號（含 demo）皆寫入 DB**；寫入後**寄信**到 `purepaw.notify@gmail.com`（沿用既有 `src/lib/email.ts` Gmail SMTP）；寄信失敗 try/catch 記 log 不影響 API 成功 — 驗證 ✅（build/review）
- [V] **信件標題區分環境**：`DATABASE_URL` 以 `file:` 開頭 → 標題前綴 `[測試]`；否則（Turso）→ `[正式]`。信件內文帶產品名/品牌/productId/回報者 userId/是否 demo/note — 驗證 ✅（review）
- [V] 前端：按鈕改呼叫該 API（沿用 handleAdd 的 createStandardizedProduct 拿 productId），成功沿用原確認訊息、失敗保持可重試 — 驗證 ⏳（待 frank 實機收信）

### 6-2 配餐項目數量可編輯（展開時）
> 設計：展開狀態（frank 圖一：項目右側帶框的「1 份」+ × 刪除）數字要可調；收合狀態（frank 圖二：灰字「(1 份)」）維持唯讀。
- [V] 後端：`meal-plans/[id]/items` 新增 **PATCH**（或建 `[itemId]` 子路由）：接 itemId+quantity，**用 requirePetAccess 依該 plan 的 pet 驗權**，更新 `MealPlanItem.quantity`（驗證 >0 數字）— 驗證 ✅（build/review）
- [V] 前端 `diet/page.tsx` SessionAccordion **展開**狀態（約 386-388）唯讀 `<span>` → 改**數字輸入**（保留「份/unit」與 × 刪除）；**改動即存**（onBlur+Enter 觸發、樂觀更新、存失敗回滾提示）— 驗證 ⏳（待實機）
- [V] **收合**狀態（約 337-339「(quantity 份)」）維持唯讀不動 — 驗證 ✅（review）
- [V] demo：配餐屬自己測試資料，數量照常寫 dev.db（非 AI 內容、不 mock）— 驗證 ✅（review）

### 6-3 修「AI 智能分析後每幾秒重載」
> 成因＝共同飼主同步輪詢 `usePollingRefresh`（25 秒＋window focus＋分頁 visible）呼叫 `fetchPlan(petId,true)` → 每次都 `setPlan(全新物件)` 重繪。**與 demo 無關，全帳號皆會。**
- [V] `diet/page.tsx` `fetchPlan` silent 重抓時**先比對資料有無實際變動**（以 `isSamePlan` 對 plan id 與各品項關鍵欄位做穩定比較），沒變就**不 setPlan**，消除無謂重繪/閃爍；AI 結果 `aiResult` 為獨立 state 不受影響 — 驗證 ⏳（待實機）

### 6-4 全域綜合飲食分析報告改造
> 現況：報告本體在 `diet/page.tsx` `DetailedReportModal`（約 600-650），含「系統分析基礎宣告 + AI 生成九大項（`DietAnalysisResult.detailedReport`，標題固定見 `DETAIL_REPORT_ITEMS`）」。
- [V] **6-4a 固定說明彈窗**：抽成獨立 `AnalysisCriteriaModal`，內容**固定寫死**（`ANALYSIS_CRITERIA` 九項靜態文案，demo/真實一致）；入口＝報告**右上角**「分析準則」按鈕（關閉鈕左側）。**原 AI 生成九大項（detailedReport）不再顯示**，由此彈窗取代 — 驗證 ✅（build/review）
- [V] **6-4b 報告本體換內容**：報告本體（`DetailedReportModal`）改為重用 `IngredientAnalysis`（風險統計列／風險總覽／營養表／補充建議／AI 營養安全風險，串 `/api/analysis` + `/api/nutrition-ai`）＋新建 `AlternativeRecommendations`（自舊 `/analysis` 移植「目前產品 vs 建議替換」塊，串 `/api/recommend`，重新以棕色系卡片風格）＋最下方 `CorrelationInsights`（串 `/api/analyze`）。demo 分流：`/api/recommend`、`/api/analyze`、`/api/nutrition-ai` 既有 gating；**`/api/analysis` 已補上 `isDemoUser` gating → 回固定 `DEMO_ANALYSIS`（`src/lib/demoAnalysis.ts`）** — 驗證 ✅（build/review）
- [V] **6-4c** `/nutrition` 與 `/analysis` 兩個分析孤兒頁**保留**（route 留、無導覽入口，本次未刪未改）；隱藏/保留項目文件之註記交 tech-writer — 驗證 ✅（review）

### 6-5 部署備註（併 0-4）
- [ ] 🔴 `ProductErrorReport` 正式庫 migration 留 deploy 執行（同 0-4 Turso migration 批次）— 驗證 ➖

### 6-6 code-review 修正（frank 拍板採納）✅
- [V] #1 `email.ts` 錯誤回報信件主旨濾換行（防 header injection）— 驗證 ✅（review）
- [V] #3 `meal-plans/[id]/items` PATCH 數量加上界 `<=9999` — 驗證 ✅（review）
- [V] #6 修 `diet/page.tsx` 3 個既有 eslint error（1007 引號未跳脫×2、1663 setState-in-effect）→ 單檔 lint 0 error — 驗證 ✅（lint 單檔）
- [V] #4 查證 `isSamePlan` tags 比較：前後端同為 JSON 字串、本就穩定 → 無需改 — 驗證 ✅

### 6-7 frank 實測再回饋修正 ✅
- [V] 營養表分頁點擊白屏 crash：根因 `nutritionAi.items` 可能 undefined → `undefined.some()` 炸。從源頭修（`/api/nutrition-ai` POST+GET 正規化成 `{overall,items:[],generalRecommendations:[]}`）+ UI 全面防護（facts/productName/items/generalRecommendations）— 驗證 ✅（build；實機待 frank）
- [V] 報告內「AI 產品替代推薦」改**先隱藏**（旗標 `SHOW_ALTERNATIVE_RECS=false`，元件 `AlternativeRecommendations.tsx` 保留不刪，未來啟用改旗標即可）— 驗證 ✅
- [V] **營養表示警邏輯修正（frank「該示警的要示警出來」）**：營養表分頁原用閹割版 `RANGES`（只 5 種、只判偏高、寫死範圍字串）→ 改採舊 `/analysis` 完整 `NUTRIENT_THRESHOLDS`（7 種粗蛋白/粗脂肪/粗纖維/水分/鈉/鈣/磷、犬貓各自 min/warn），抽成模組常數。依 `data.pet.species`（貓→cat、其餘→dog）選門檻；**雙向示警**：>warn→「警示」(橘⚠️)、<min→「不足」(藍▾)、之間→「正常」(綠)、無門檻→「—」不誤判；建議範圍欄由 min/warn 動態組（如「≥18% <40%」）；`ABadge` 新增 `low`(藍) 狀態。demo 走同套（demo pet species「狗」→dog）。`tsc --noEmit`／`build` 綠燈、改動檔 lint 0 error（僅既有 `<img>` warning）。work-report `docs/work-reports/phase6-nutrition-table-fix-fullstack.md` — 驗證 ✅（build；實機待 frank）

### 6-8 緩做項（記錄避免遺漏，frank 說做再做）
- [ ] **全專案 lint 債**：`npm run lint` 全專案約 2237 個**既有** error（`eslint-config-next` react-hooks 規則對數十個既有元件報的，非 Phase 6 引入）。本次指定 3 個已清、改動檔單獨 lint 乾淨、build 不受影響。需另開大型重構單，**frank 指示再做** — 驗證 ➖
- [ ] #2 開報告時 `/api/analysis` 被重複 GET 兩次（`IngredientAnalysis` + `AlternativeRecommendations` 各抓一次；後者現已隱藏，暫無此問題）→ 未來重新啟用替代推薦時，父層抓一次傳 prop — 驗證 ➖
- [ ] 🟡 review 建議：環境前綴改顯式旗標、analysis GET 失敗靜默無區分、刪除失敗未回滾（與數量編輯回滾不一致）、demo `expertComment` 醫療性措辭（轉 copywriter/legal 審）— 驗證 ➖

---

## 進度總覽

| Phase | 主題 | 優先 | 估時 | 狀態 | 驗證 |
|-------|------|------|------|------|------|
| 0 | 快速清理與補資料 | P1/P3 | 半天 | ✅ 完成（0-1 保留；正式庫 migration 留 deploy 執行）| 部分✅/➖ |
| 1 | 共同飼主 Email 邀請（含權限修復、輪詢同步） | P1 | 2～3 天 | 🔄 1-A~1-E 完成；1-F 待 frank 實機端對端 | 1-B~1-E ✅；1-A ➖；1-F ⏳ |
| 2 | 首頁資料串接 + 日誌/飲食修正 | P1 | ~1 天 | ✅ 2-1~2-8 完成（2-6 真機待驗） | 2-1~2-5/2-7/2-8 ✅；2-6 真機待 frank |
| 3 | 既有 UI 功能補完 + 導覽/版面調整 | P2 | 2～3 天 | ✅ 3-1/3-3~3-10 完成（3-2 併 Phase 5）| 多數 ✅；3-3 語音/拍照/AI/3-6 SOS 清單 待實機/補料 |
| 4 | AI 加值與空殼內容化 | P2 | 1～1.5 天 | ✅ 4-1~4-6 完成 | review+build+設計圖 ✅；AI 輸出/相機 待實機 |
| 5 | 主動推播：食安警報 + 用藥/美容提醒（Web Push）| P2 | 2～3 天 | ✅ 5-A~5-E 完成（review 5 項修畢）| review+build ✅；實機收推播/iOS 待 frank ⏳ |
| 6 | 飲食頁測試回饋修正（錯誤回報/數量編輯/重載/報告改造）| P1 | ~1 天 | 🔄 6-0/6-1 完成待 commit；6-2~6-4 進行中 | 6-1 ✅（build/review）；其餘 ⏳ |

---

## 附錄：本次盤點補充驗證的事實
- `requirePetAccess` 已存在於 `src/lib/petAccess.ts`，但目前僅 `invitations`、`members` 兩支 API 使用；其餘約 25 支寵物資料 API 皆未做權限檢查（Phase 1-C 全列）。
- seed.ts 已建立 9 個 model，缺 8 個（Phase 0-3 全列）。

*P2 後續（撤銷/刪除邀請、邀請歷史分頁、通知發起者）見 `docs/未來功能.md`。*
*完成項目請更新勾選與進度總覽。*
