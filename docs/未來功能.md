# 未來功能 Backlog

本文件記錄已確認、但**不在當期範圍**的功能項目，供後續排期參考。
每項註明來源、原因與優先度。

---

## 共同飼主相關（來自 2026-06-03 共同飼主 Email 邀請流程討論）

> 背景：當期只做「Email 邀請寄信 + 共同飼主存取修復 + 共享資料即時同步」。
> 以下項目經需求方確認**移出當期範圍**，列為 P2 後續。
> 相關現況見 `docs/系統盤點-2026-06-03.md` 第七節。

| # | 功能 | 說明 | 優先 |
|---|-----|------|------|
| 1 | 撤銷 / 刪除邀請 | owner 可手動撤回尚未接受的待處理邀請（如 `DELETE /api/pets/[id]/invitations/[invitationId]`）；目前邀請送出後無法收回 | P2 |
| 2 | 邀請歷史分頁 | 在共同飼主區塊新增「已接受 / 已過期」歷史紀錄檢視，與「待接受」分開 | P2 |
| 3 | 通知發起者 | 受邀者接受邀請時，主動通知原邀請人（站內訊息或 email） | P2 |

---

## 其他（來自 2026-06-04 Phase 0 收尾）

| # | 功能 | 說明 | 優先 |
|---|-----|------|------|
| 4 | 歷史觀察計畫顯示 | `WeeklyTask`（每週健康觀察計畫）目前有資料模型與 seed，但全站無顯示頁面。未來在 `/nutritionist` 或專屬頁顯示歷史觀察計畫。seed 先保留彈性，需要時再做 | P3 |
| 4b | 產品評分（吃後感想） | v1 功能，現版暫時不需要。元件 `DailyReactionCard` 已保留，掛在飲食頁 `/diet` 但以旗標 `SHOW_PRODUCT_REACTIONS=false` 隱藏；對使用中產品逐項記錄好/一般/不好（`ProductReaction`，沿用 `/api/reactions`）。未來要啟用把旗標改 true 即可 | P3 |
| 5 | 週曆跨週導覽 | `/diary` 週曆目前無法切到上一週/下一週，跨週的當日紀錄（如評分）只能用月曆查看 | P3 |
| 6 | 全站 API 擁有者驗證 | ✅ 大部分已於 Phase 1-C 完成（~30 支寵物資料 API 加 `requirePetAccess`）。**殘留**見 #10/#11/#12 | P2 |
| 7 | ProfileMenu 抽共用元件 | 手機（AppShell）與桌面（Sidebar）的 ProfileMenu 為複製貼上，建議抽 `<ProfileMenu>` 共用 | P3 |
| 8 | lint 收斂 | Next 16 / React 19 新規則 `react-hooks/set-state-in-effect` 對全站既有 effect-setState 慣例報 error，建議統一處理 | P3 |
| 9 | 日誌月曆總覽版面重新設計 | 當期只修 bug + 加白話摘要（見待辦 2-3）。總覽要呈現哪些資訊、版面如何讓使用者一眼看懂，frank 預期之後會再大改 | P2 |
| 10 | 未納管 API rate-limit | `extract`/`upload`/`products`（含 web-search）/`news`/`users` 不綁單一寵物、無 IDOR，但無 auth/rate-limit，可被匿名濫用燒 token / 任意存檔。建議加登入要求 + rate-limit | P2 |
| 11 | 未登入/匿名寵物策略 | `userId=null` 的匿名寵物套 `requirePetAccess` 會 403；`/api/pets` GET 未登入仍回資料。需定義匿名策略（正式只開 Google 登入後此問題自然收斂） | P2 |
| 12 | 邀請防呆/小修 | ~~(a) 「已是成員」防呆 email 大小寫漏判~~ ✅ 已修（改抓全成員、email 轉小寫比對）；(b) 建立寵物 + owner membership 未包 transaction（雙重判定下影響極小，待補） | P3 |

---

*文件由 Claude Code 建立，記錄已確認的後續功能。新增項目時請註明來源與優先度。*
