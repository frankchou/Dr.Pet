# 系統架構

> 最後更新：2026-06-03（飲食頁 bug 修正 + 營養分析頁 + 導覽列調整）

---

## 產品概覽

**PurePaw 無敏毛孩** 是專為台灣飼主打造的 AI 寵物營養健康管理平台，核心功能為即時掃描寵物食品成分、AI 個人化飲食建議、健康日誌追蹤。前身品牌為「寵物隨行醫師 Dr. Pet」，v2.0.0 起全面更名。

---

## 技術棧

| 層級 | 技術 | 版本 / 備注 |
|------|------|------------|
| 框架 | Next.js App Router | v16，TypeScript |
| 資料庫 ORM | Prisma | v7，`@prisma/adapter-libsql` + `@libsql/client` |
| 資料庫引擎 | SQLite（開發）/ Turso（生產計畫） | `DATABASE_URL` 不寫在 `schema.prisma`，由 `prisma.config.ts` 注入 |
| UI 樣式 | Tailwind CSS | v4，PostCSS plugin，無 `tailwind.config.js` |
| 圖表 | Recharts | 須 `dynamic(..., { ssr: false })` |
| AI | Anthropic SDK | `claude-sonnet-4-6` |
| 字體 | Quicksand（英數）+ Noto Sans TC（中文） | 由 Google Fonts CDN 載入 |
| 排程 | Vercel Cron | `vercel.json` 設定，每日 UTC 02:00 觸發快訊爬蟲 |

---

## 版面架構（RWD）

```
root layout (src/app/layout.tsx)
├── ClientShell          ← 身份驗證 + 免責聲明守衛
│   ├── Sidebar          ← 桌面左側欄（md 以上顯示，w-64 fixed）
│   └── main content area
│       ├── {page children}
│       └── BottomNav    ← 手機底部導覽列（md 以下顯示）

/landing（獨立 layout，不套用 App shell）
└── landing/layout.tsx   ← 覆蓋父層 layout，僅回傳 children，無 Sidebar/BottomNav/ClientShell
```

Landing page 使用 `src/app/landing/layout.tsx` 覆蓋父層 layout，完全跳過 `ClientShell`、`Sidebar`、`BottomNav`，適合對外公開的行銷頁面。

**手機（< md）**：底部導覽列 `BottomNav`，高度 60px，左側「毛孩 / 飲食 / 日誌」（3 tabs）+ 中央凸起相機 FAB + 右側「營養 / AI諮詢 / 快訊」（3 tabs），共 7 個項目。  
**桌面（>= md）**：左側欄 `Sidebar`，寬 256px fixed；主內容區 `ml-64` 偏移；導覽項目：毛孩 / 飲食 / 日誌 / 照相 / 營養分析 / AI諮詢 / 快訊。

---

## 路由結構

| 路徑 | 頁面 | 說明 |
|------|------|------|
| `/` | 毛孩（首頁 Dashboard） | 重要日程（頂部）、Pet Hero 卡片、健康檔案概覽、未來日程表、健康指標、快速功能（最底） |
| `/nutrition` | 營養分析 | 寵物產品成分風險報告（IngredientAnalysis 元件），從首頁移出獨立成頁 |
| `/diet` | 飲食頁 | 日常配餐 Tab（晨/午/晚 Accordion + 食物 CRUD）+ 換食計畫 Tab + AI 智能分析配餐 |
| `/diary` | 日誌 | 月/週曆、AI 隨記、用藥看診、洗澡美容、日常飲食紀錄、使用中產品、換食計畫 |
| `/scan` | 即時成分掃描分析 | 三階段 Modal（capture→analyzing→result）+ 歷史記錄列表 |
| `/nutritionist` | AI 諮詢 | AI 對話、建議題、觀察計畫、多寵物切換 |
| `/news` | 快訊 | 三 Tab（食安警報/危險禁忌/健康知識），從 DB 動態讀取 NewsArticle |
| `/landing` | 行銷落地頁 | Hero/Marquee/Stats/About/Features/How it works/FAQ/CTA/Footer；獨立 layout |
| `/settings` | 設定頁 | 毛孩檔案（滑動卡片 + CRUD）+ 紀錄參數設定（13 toggle） |
| `/pet`, `/pet/new`, `/pet/[id]` | 寵物檔案管理 | 寵物基本資料 CRUD |
| `/analysis` | 成分知識庫分析 | 規則式成分分析（不呼叫 AI） |

### v1 路由重新導向（永久 301）

`next.config.ts` 設定 7 條永久重新導向，確保 v1（Dr. Pet）時期的舊網址不會 404：

| 舊路由（v1） | 新路由（v2） |
|------------|------------|
| `/log` | `/diary` |
| `/log/new` | `/diary` |
| `/chat` | `/nutritionist` |
| `/products` | `/diary` |
| `/symptoms` | `/diary` |
| `/symptoms/new` | `/diary` |
| `/upload` | `/scan` |

### 日誌頁技術決策：用藥與美容改用 `/api/symptoms`

日誌頁的「用藥與看診」和「洗澡美容」兩個功能，原本預計透過 `/api/usages` 儲存記錄，但 `usages` 資料表的 `productId` 欄位為必填（NOT NULL），無法在沒有綁定產品的情況下建立記錄（例如：純粹記錄「洗澡」這件事而不關聯特定產品）。因此改用 `/api/symptoms`，以 `symptomType: 'medication'` 和 `symptomType: 'grooming'` 作為類型區分，彈性更大、不強制關聯產品。

---

## API Endpoints

| 路徑 | 方法 | 功能 |
|------|------|------|
| `/api/extract` | POST | 視覺 AI 擷取食品標籤成分 |
| `/api/instant-analyze` | POST | 照片 → 成分安全判讀（safe/caution/danger），存 `InstantAnalysis` |
| `/api/chat` | POST | AI 對話，注入寵物背景資料為 system prompt |
| `/api/analyze` | POST | 相關性分析，存 `AIInsight` |
| `/api/nutrition-ai` | POST | AI 營養摘要（產品堆疊） |
| `/api/recommend` | POST | AI 產品推薦 |
| `/api/tasks` | POST | 生成每週照護任務清單 |
| `/api/analysis` | GET | 規則式成分知識庫分析（不呼叫 AI） |
| `/api/pets` | CRUD | 寵物資料（GET 依 session userId 過濾，只回傳該用戶的寵物）|
| `/api/meal-plans` | GET/POST | 每日配餐計畫（DailyMealPlan）CRUD |
| `/api/meal-plans/[id]/items` | POST/DELETE | 配餐項目（MealPlanItem）新增/刪除 |
| `/api/diet-analysis` | POST | AI 配餐分析，回傳評分/指標/警示/建議/詳細報告 |
| `/api/news` | GET/POST | 快訊文章讀取（按 category 過濾）/新增 |
| `/api/news/crawl` | POST | AI 爬蟲（Cron 觸發）生成快訊文章寫入 DB |
| `/api/symptoms` | CRUD | 症狀記錄（含 grooming / medication type）|
| `/api/pet-products` | CRUD | 寵物產品清單 |
| `/api/reactions` | CRUD | 每日產品反應評分 |
| `/api/health-metrics` | GET/POST | 每日健康指標（BCS/活力/水分）|
| `/api/diary-records` | GET | 依日期查詢該天所有紀錄（症狀+使用+健康指標）|
| `/api/diary-parse` | POST/PUT | AI 解析隨記文字 → 結構化紀錄 → 確認後儲存 |
| `/api/products/web-search` | POST | AI + web_search 搜尋真實寵物食品並做成分分析 |
| `/api/meal-plans` | GET/POST | 取得或建立當日配餐計畫（DailyMealPlan） |
| `/api/meal-plans/[id]/items` | POST/DELETE | 新增或刪除配餐項目（MealPlanItem） |
| `/api/diet-analysis` | POST | AI 配餐綜合分析，回傳評分/指標/警示/建議/通路推薦/9大報告 |
| `/api/news` | GET/POST | 快訊文章讀取與新增（NewsArticle） |
| `/api/news/crawl` | POST | AI 生成新聞文章並寫入 DB（需 CRON_SECRET 授權，Cron 觸發） |

---

## 認證方式

**現況：雙軌登入**

1. **Mock Google 登入**：點擊「使用 Google 繼續」按鈕後直接寫入 localStorage，不發出真實 OAuth 請求（正式環境預設）。
2. **測試帳號登入（DEMO 模式）**：`DEMO_ENABLED=true` 時，NextAuth 啟用 `CredentialsProvider`，使用帳號 demo@drpet.com + 密碼 demo1234 可登入。前端以 `NEXT_PUBLIC_DEMO_ENABLED` 控制測試帳號表單是否顯示。正式環境不設定此環境變數，入口完全不出現。

| localStorage key | 值 | 說明 |
|---|---|---|
| `purepaw_user_ack` | `'1'` | 使用者已完成登入（mock） |
| `drpet_userId` | `'local_user'` | 用戶 ID（mock 固定值） |
| `drpet_nickname` | 字串 | 顯示名稱 |
| `purepaw_disclaimer_ack_v1` | `'1'` | 使用者已閱讀並同意免責聲明 |
| `drpet_currentPetId` | 字串 | 目前選取的寵物 ID |

**後續計畫**：串接真實 Google OAuth，`purepaw_user_ack` 改由 token 驗證取代。

---

## 資料庫架構重點

- `Pet` 為核心實體，幾乎所有模型均以 Pet delete 為級聯刪除對象。
- 陣列型欄位（如 `mainProblems`、`allergies`、`photos`）儲存為 JSON 字串，讀取須用 `parseJson()`（位於 `src/lib/utils.ts`）。
- `PetProduct` 管理寵物的固定／試用產品清單（`listType: "fixed" | "trial"`）。
- `ProductReaction` 記錄每日產品反應評分，唯一約束 `@@unique([petId, productId, date])`。
- `HealthMetric` 記錄每日健康指標（BCS 體態評分 1–9、活力 low/medium/high、水分 low/medium/high），唯一約束 `@@unique([petId, date])`。
- `DailyMealPlan` 記錄寵物每日配餐計畫，唯一約束 `@@unique([petId, date])`；每筆計畫包含多個 `MealPlanItem`。
- `MealPlanItem` 為配餐中的單一食物項目，以 `session`（morning / noon / evening）區分三餐歸屬。
- `NewsArticle` 儲存快訊文章，以 `category`（food_safety / danger / health）分類，由 AI 爬蟲定期寫入。
- Prisma v7 的 `DATABASE_URL` 不放在 `schema.prisma`，改由 `prisma.config.ts` 設定，runtime 於 `src/lib/prisma.ts` 建立帶 libsql adapter 的 `PrismaClient`。

---

## Cron Job 排程

`vercel.json` 設定 Vercel Cron，每日自動觸發快訊爬蟲：

| Cron 時間（UTC） | 台灣時間 | 目標 API | 功能 |
|---|---|---|---|
| `0 2 * * *` | 每日 10:00 | `POST /api/news/crawl` | AI 生成當日寵物相關新聞文章並寫入 DB |

呼叫時須在 HTTP Header 帶入 `Authorization: Bearer {CRON_SECRET}` 進行驗證，防止未授權呼叫。

---

## Mock 資料 Seed

`prisma/seed.ts` 提供開發用示範資料，執行後建立：

- 示範使用者 `demo@drpet.com` 及其寵物「布丁」
- 布丁今日配餐計畫（晨食 / 午食 / 晚食各含代表性食物項目）
- 7 筆示範新聞文章（food_safety / danger / health 各類別）

---

## 設計令牌

定義於 `src/app/globals.css` 的 CSS 變數：

| 變數名 | 值 | 用途 |
|--------|-----|------|
| `--pp-font` | Quicksand / Noto Sans TC | 全站字體 |
| `--pp-canvas` | `#F4F7FB` | 頁面背景色 |
| `--pp-apricot` | `#FFE8D6` | 品牌杏色（Logo 背景、裝飾區塊） |
| `--pp-apricot-dark` | `#D98A53` | 品牌深杏色（強調按鈕、圖示） |
| `--pp-ink` | `#111111` | 主要文字、深色按鈕 |
| `--pp-card` | `#FFFFFF` | 卡片背景 |
| `--pp-muted` | `#64748B` | 次要文字 |
| `--pp-shadow-nav` | `0 -10px 40px rgba(0,0,0,0.04)` | 底部導覽陰影 |
| `--pp-shadow-pop` | `0 8px 32px rgba(0,0,0,0.12)` | 彈窗陰影 |

---

## 環境變數

```
DATABASE_URL=file:./dev.db          # 開發用 SQLite，runtime 轉為絕對路徑
ANTHROPIC_API_KEY=sk-ant-...        # Anthropic AI 金鑰
DEMO_ENABLED=true                   # 啟用測試帳號（開發環境，正式環境不設定）
NEXT_PUBLIC_DEMO_ENABLED=true       # 前端顯示測試帳號登入表單（對應 DEMO_ENABLED）
CRON_SECRET=...                     # Cron Job 授權密鑰（保護 /api/news/crawl）
```

---

## 關鍵共用模組

| 檔案 | 功能 |
|------|------|
| `src/lib/prisma.ts` | PrismaClient singleton（libsql adapter） |
| `src/lib/anthropic.ts` | Anthropic client singleton |
| `src/lib/utils.ts` | `cn()`、`parseJson()`、`formatDate()`、`VET_REFERENCE_SCOPE` 等 |
| `src/lib/ingredientAnalyzer.ts` | 規則式成分分類引擎（toxic/warning/caution/safe） |
| `src/lib/ingredientKnowledge.ts` | 成分知識庫資料 |
| `src/types/index.ts` | TypeScript 介面定義 |
