# 隱藏與保留項目清單

本文件登記「**程式碼已存在、但目前刻意隱藏／保留待未來啟用**」的功能與區塊，方便日後要開回來時一次找到「在哪、怎麼開」。

> 狀態說明：
> - ✅ **已隱藏**：目前程式碼中已經隱藏／不顯示。
> - ⏳ **待隱藏**：已排進待辦（Phase 3），尚未實作。

最後更新：2026-06-10（Phase 6）

---

## 一、頁面層級（整頁隱藏）

| 項目 | 位置 / 路由 | 隱藏方式 | 如何重啟 | 狀態 | 來源 |
|-----|-----------|---------|---------|------|------|
| 營養分析頁 | `/nutrition`（電腦左側 nav、手機下方「營養」） | nav 移除入口（頁面程式保留） | nav 加回入口 | ✅ 已隱藏（孤兒頁，route 留、無導覽入口） | 待辦 3-4 / 6-4c |
| 成分知識庫分析頁 | `/analysis` | 無導覽入口（頁面程式保留） | nav 加回入口 | ✅ 已隱藏（孤兒頁，route 留、無導覽入口） | 待辦 6-4c |
| AI 諮詢頁 | `/nutritionist` | nav 移除入口；功能改掛日誌頁浮動 modal | nav 加回入口（或保留日誌入口） | ✅ 已隱藏 | 待辦 3-4 / 3-5 |
| 舊版 AI 諮詢頁（v1 死頁） | `/chat`（`src/app/chat/page.tsx`） | 無任何入口連結，已加棄用註解，孤立保留 | 不建議重啟（已被 `/nutritionist` 取代），後續可直接刪 | ✅ 已隱藏（孤立） | 待辦 0-1 |

> **Phase 6 註記**：`/nutrition`、`/analysis` 兩個分析孤兒頁**保留**（route 留、無導覽入口，未刪未改）。其中「成分綜合分析」的顯示格式（風險總覽／營養表／補充建議／AI 營養安全風險）已透過 `IngredientAnalysis` 元件**重用進飲食頁全域綜合飲食分析報告本體**；孤兒頁本身屬重複入口，故無導覽入口、保留待未來決定去留。

---

## 二、區塊 / 元件層級

| 項目 | 位置 / 檔案 | 隱藏方式 | 如何重啟 | 狀態 | 來源 |
|-----|-----------|---------|---------|------|------|
| 產品評分（吃後感想） | `DailyReactionCard`，掛在飲食頁 `/diet` | 旗標 `SHOW_PRODUCT_REACTIONS = false` | 把旗標改 `true` | ✅ 已隱藏 | 未來功能 #4b |
| 首頁「健康指標」區塊 | `src/app/page.tsx` | 區塊暫時隱藏、代碼保留 | 取消隱藏該區塊 | ✅ 已隱藏（需實機覆核） | 版本紀錄 2026-06-03 16:28 |
| 健康檔案概覽－活力指數 | `src/app/page.tsx`（毛孩頁「健康檔案概覽」） | 旗標 `SHOW_VITALITY_HYDRATION = false`（代碼保留） | 把旗標改 `true` | ✅ 已隱藏 | 待辦 3-10 |
| 健康檔案概覽－水分攝取 | `src/app/page.tsx`（毛孩頁「健康檔案概覽」） | 旗標 `SHOW_VITALITY_HYDRATION = false`（代碼保留） | 把旗標改 `true` | ✅ 已隱藏 | 待辦 3-10 |
| AI 產品替代推薦 | `AlternativeRecommendations`（`src/components/nutrition/AlternativeRecommendations.tsx`），掛在飲食頁 `/diet` 全域綜合飲食分析報告本體 | 旗標 `SHOW_ALTERNATIVE_RECS = false`（`diet/page.tsx`，條件渲染、元件保留不刪） | 把旗標改 `true` 即可啟用 | ✅ 已隱藏 | 待辦 6-7 |

---

## 三、有資料 / 模型但尚無顯示端（保留待未來）

| 項目 | 說明 | 如何啟用 | 狀態 | 來源 |
|-----|------|---------|------|------|
| 歷史觀察計畫（WeeklyTask） | 有資料模型與 seed，但全站無顯示頁面 | 未來在 `/nutritionist` 或專屬頁做顯示 | ✅ 無消費端（seed 保留彈性） | 未來功能 #4 |

---

## 四、非「隱藏」但相關：功能未完成而停用（disabled）

> 以下是「按鈕在、但因功能尚未實作而停用」，與「刻意隱藏」性質不同，一併列出避免混淆。

| 項目 | 位置 | 狀態 | 來源 |
|-----|------|------|------|
| 頭像上傳 | `settings/page.tsx` | 按鈕 disabled（功能未做） | 待辦 3-1 |
| SmartMemo 語音輸入 | `diary/page.tsx` | 按鈕 disabled（功能未做） | 待辦 3-3 |

---

*本文件由 Claude Code 整理。新增隱藏項目時，請同步更新此表與對應待辦。*
